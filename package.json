{
  "name": "scientist-workbench",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "workspaces": [
    "packages/*",
    "tools/*"
  ],
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "check": "bun scripts/check.ts",
    "check:quick": "bun scripts/check.ts --quick",
    "goldens": "bun scripts/generate-goldens.ts",
    "goldens:check": "bun scripts/generate-goldens.ts --check",
    "gen-catalog": "bun scripts/gen-catalog.ts",
    "gen-tool-readme": "bun scripts/gen-tool-readme.ts",
    "new-tool": "bun scripts/new-tool.ts",
    "wb": "bun wb.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/bun": "^1.3.0"
  }
}
