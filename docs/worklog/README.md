# Worklog

A sharded log of substantive work on `scientist-workbench`. One shard per
discrete iteration; each is self-contained so a reader landing cold on a
shard understands what changed and why.

The point is **continuity**: future-you, future agents, and future
collaborators reach for the worklog when "git blame says I changed this
line, but why?" That's what these shards are for. Keep them honest —
write the frictions and the dead ends as well as the wins.

| #   | Title                                                                     | Date       | Issues                          |
|-----|---------------------------------------------------------------------------|------------|---------------------------------|
| 001 | [NTT port from tstournament 02-NTT](001-ntt-port-from-tstournament.md)    | 2026-04-28 | (port; surfaced 9 frictions)    |
| 002 | [F1+F2 — subprocess plumbing centralised](002-spawn-machinery.md)         | 2026-04-28 | scientist-workbench-rpb.1       |
| 003 | [F3 — scaffolder accepts `--uses`](003-scaffolder-uses.md)                | 2026-04-28 | scientist-workbench-rpb.2       |
| 004 | [F8 — schema `kindOf` annotations](004-schema-kind-annotations.md)        | 2026-04-28 | scientist-workbench-rpb.7       |
| 005 | [F7 — `@workbench/json-bridge` package](005-json-bridge.md)               | 2026-04-28 | scientist-workbench-rpb.6       |
| 006 | [F5 — output error patterns + mod-inv migration](006-error-patterns.md)   | 2026-04-28 | scientist-workbench-rpb.4       |
| 007 | [F6 + F4 + F9 — lint, example-count, TDD shapes](007-conventions-and-docs.md) | 2026-04-28 | scientist-workbench-rpb.{5,3,8} |
| 008 | [Schema as a first-class type](008-schema-as-first-class-type.md)         | 2026-04-28 | scientist-workbench-{ktd,73m,7q0,1d9} |
| 009 | [Sturm-TS port: planning shard](009-sturm-ts-port-planning.md)            | 2026-04-29 | scientist-workbench-{i8m,x9x,cdz,0lo,dwg,z8w,tkx,564,kw1,bir,q0b,733,8e8,o1q,can} (planned) |
| 010 | [ADR 0005: externalised entropy](010-externalised-entropy.md)             | 2026-04-29 | scientist-workbench-i8m         |
| 011 | [ADR 0006: IR-as-Value encoding](011-ir-as-value.md)                      | 2026-04-29 | scientist-workbench-x9x         |
| 012 | [ADR 0007: distribution-vs-sampling](012-distribution-vs-sampling.md)     | 2026-04-29 | scientist-workbench-cdz         |
| 013 | [Sturm-TS v3.1 spec amendment](013-sturm-ts-spec-v3-1.md)                 | 2026-04-29 | scientist-workbench-0lo         |
| 014 | [packages/sturm-ir](014-packages-sturm-ir.md)                             | 2026-04-29 | scientist-workbench-dwg         |
| 015 | [tools/sturm-simplify](015-sturm-simplify.md)                             | 2026-04-29 | scientist-workbench-z8w         |
| 016 | [cas-core ring-generic refactor](016-cas-core-ring-generic.md)            | 2026-04-29 | scientist-workbench-{but,t87}   |
| 017 | [cas-core algebraic numbers](017-cas-core-algebraic-numbers.md)           | 2026-04-29 | scientist-workbench-1s4         |
| 018 | [tools/sturm-execute (v0.1 float64)](018-sturm-execute.md)                | 2026-04-29 | scientist-workbench-tkx (jfj filed) |
| 019 | [tools/sturm-equivalent (Phase 1 killer demo)](019-sturm-equivalent.md)   | 2026-04-29 | scientist-workbench-564             |
| 020 | [tools/entropy-source (Phase 2 kick-off)](020-entropy-source.md)          | 2026-04-29 | scientist-workbench-kw1             |
| 021 | [tools/sturm-sample (Born's rule applied)](021-sturm-sample.md)           | 2026-04-29 | scientist-workbench-bir             |
| 022 | [Sturm-TS v3 spec absorbed; §8.1 H verified buggy](022-spec-v3-absorption-and-h-verification.md) | 2026-04-29 | scientist-workbench-{4xk closed; 1td, r40, 4iw filed} |
| 023 | [Channel combinators (sturm-controlled, sturm-then, sturm-tensor)](023-channel-combinators.md) | 2026-04-29 | scientist-workbench-o1q             |
| 024 | [TS-native frontend DSL: agents-as-TS-experts is the spec](024-ts-native-frontend-dsl.md) | 2026-04-30 | (none — beads db not initialised; ADR-0009 is the spec) |
| 025 | [Grover end-to-end via @workbench/sturm + sturm-lib + sturm-find](025-grover-end-to-end.md) | 2026-04-30 | (none — beads db not initialised) |
| 026 | [Code-health pass: protocol DRY, dead code, literate ntt](026-code-health-protocol-dry.md) | 2026-05-02 | scientist-workbench-{9s4, cji, y8p, 10w, hgc, 61s} |
| 027 | [Multi-device beads sync via tracked git hooks](027-multi-device-beads-sync.md) | 2026-05-02 | (infra; closes the worklog 024/025 tracker drift) |
| 028 | [defineTool / runTool split, registry without spawning](028-define-runtool-split.md) | 2026-05-03 | scientist-workbench-yth |
| 029 | [Typed flag declarations on ToolDefinition](029-typed-flags.md) | 2026-05-03 | scientist-workbench-rej (5gl filed as follow-up) |
| 030 | [Polynomial GCD in cas-core](030-polynomial-gcd.md) | 2026-05-03 | scientist-workbench-djr (6uc filed as v0.2 modular follow-up) |
| 031 | [First numerical tier — linalg-core + linalg-solve (ADR-0014)](031-first-numerical-tier.md) | 2026-05-03 | scientist-workbench-n2a (epic), -abj (ADR), -0ky (pkg), -ynd (tool), -gyb (docs), -bf0 (this shard); follow-ups -71f -wmm -0ck -e7y -va1 |
| 032 | [Composition layer MVP (`@workbench/compose`) + provenance lockstep (ADR-0012)](032-composition-layer-mvp.md) | 2026-05-03 | scientist-workbench-{c24, inm, 9n1, 23i, o8t} closed; remaining: -46z, -4t5, -mtw, -csa, -e0h |
| 033 | [Typed barrel for `@workbench/compose` (`wb.modPow({...})`)](033-typed-barrel.md) | 2026-05-03 | scientist-workbench-4t5 |
| 034 | [`Workbench.lookup` + `runMemoized`: cache by input hash](034-lookup-and-runMemoized.md) | 2026-05-03 | scientist-workbench-{mtw, csa} |
| 035 | [Fluent `wb.pipe(...)` + demo-scope.ts migration (full DAG closed)](035-pipe-and-demo-migration.md) | 2026-05-03 | scientist-workbench-{46z, e0h} |
| 036 | [ADR-0015: determinism tier (numerical contract relaxation)](036-determinism-tier.md) | 2026-05-04 | scientist-workbench-0ck (closes); -auz, -2t4 filed |
| 037 | [ADR-0015 implementation: numerical tier wired end-to-end](037-determinism-tier-implementation.md) | 2026-05-04 | implementation behind 0ck (already closed); 24 new tests, 3 mutations proven |
| 038 | [oracle: return record on every path; CI exits via output inspection](038-oracle-throw-not-exit.md) | 2026-05-04 | scientist-workbench-qf1 |
| 039 | [`integrate-1d` ships; orchestration meta-experiment yields code-vs-summary divergence](039-integrate-1d-and-orchestration-experiment.md) | 2026-05-04 | (none — meta-experiment + tool ship) |
| 040 | [`optimize-lbfgs-projected` ships: third numerical-tier tool, L-BFGS-B class](040-optimize-lbfgs-projected.md) | 2026-05-04 | (none filed) |
| 041 | [`cas-diff` ships: symbolic differentiation over the closed numerical vocabulary](041-cas-diff.md) | 2026-05-04 | scientist-workbench-cnv |
| 042 | [tier-1 vocabulary extension: inverse trig + hyperbolics + log bases](042-tier1-vocab-extension.md) | 2026-05-04 | scientist-workbench-0jn |
| 043 | [`linalg-qr` via the tstournament-protocol bench (49/49, 343/343)](043-linalg-qr-via-bench.md) | 2026-05-05 | scientist-workbench-3jq |
| 044 | [`linalg-svd` via the tstournament-protocol bench (49/49, 392/392)](044-linalg-svd-via-bench.md) | 2026-05-05 | scientist-workbench-c03 |
| 045 | [Numerical-tier `n` cap lift (ADR-0016) + NIST industrial benchmarks](045-numerical-tier-cap-lift-and-industrial-bench.md) | 2026-05-05 | scientist-workbench-32s |
| 046 | [`linalg-svd` Golub-Reinsch path (dual-algorithm dispatch by size)](046-svd-golub-reinsch.md) | 2026-05-05 | scientist-workbench-y9u |
| 047 | [`linalg-eigh` via the tstournament-protocol bench (46/46, 316/316)](047-linalg-eigh-via-bench.md) | 2026-05-05 | scientist-workbench-evb |
| 048 | [`integrate-ode-ivp` via the tournament-protocol bench (29/29)](048-integrate-ode-ivp.md) | 2026-05-05 | scientist-workbench-l6p |
| 049 | [`integrate-ode-stiff` via the tournament-protocol bench (19/19)](049-integrate-ode-stiff.md) | 2026-05-05 | scientist-workbench-09g |
| 050 | [`integrate-ode-symplectic` via the tournament-protocol bench (17/17)](050-integrate-ode-symplectic.md) | 2026-05-05 | scientist-workbench-4gr |
| 051 | [`poly-factor-q` bench + first substrate (Yun square-free, 17 tests / 607 expects)](051-poly-factor-bench-and-squarefree.md) | 2026-05-06 | scientist-workbench-{4nz, 3s2, 153} |
| 052 | [`poly-factor` end-to-end: Hensel + Berlekamp + recombination + tool ship (Phase 2 closes)](052-poly-factor-end-to-end.md) | 2026-05-06 | scientist-workbench-{0fy, p3d, 5k6, v13, d0o} |
| 053 | [`poly-roots`: closed-form radical roots for deg ≤ 4 (Cardano + Ferrari)](053-poly-roots.md) | 2026-05-06 | scientist-workbench-{1yu, 58q} |
| 054 | [`solve`: top-level dispatcher (linear + univariate-poly v0.1)](054-solve-dispatcher.md) | 2026-05-06 | scientist-workbench-{77b, cfd, fij, 80x} |
| 055 | [`solve` transcendental invert layer + linear-arg compound (sin(2x+1)…)](055-transcendental-invert.md) | 2026-05-06 | scientist-workbench-{ii0, 37r} |
| 056 | [`bench/solve` headline bench (yq2)](056-bench-solve-headline.md) | 2026-05-07 | scientist-workbench-yq2 |
| 057 | [`bench/poly-roots-radical` (iyj) + demo-scope solve entries (b22)](057-bench-poly-roots-radical.md) | 2026-05-07 | scientist-workbench-{iyj, b22} |
| 058 | [`bench/real-root-isolate` (q8q): VAS-LMQ bench](058-bench-real-root-isolate.md) | 2026-05-07 | scientist-workbench-q8q |
| 059 | [`packages/real-roots` + `tools/real-root-isolate` (rra): VAS-LMQ ship](059-real-roots-vas-lmq.md) | 2026-05-07 | scientist-workbench-rra |
| 060 | [alg-num: `Root[poly, k]` type + canonicalisation (xyt)](060-alg-num-root-type.md) | 2026-05-07 | scientist-workbench-xyt |
| 061 | [alg-num: `refineRoot` (xkz) + `makeRootByIndex` (6cd)](061-alg-num-refine-and-byindex.md) | 2026-05-07 | scientist-workbench-{xkz, 6cd} |
| 062 | [alg-num: resultant arithmetic on Roots (rti)](062-alg-num-arithmetic.md) | 2026-05-07 | scientist-workbench-rti |
| 063 | [`tools/poly-roots` deg-≥5 lift: `Root[]` for irreducible quintics+ (yoc)](063-yoc-poly-roots-deg5.md) | 2026-05-07 | scientist-workbench-yoc |
| 064 | [`tools/solve` deg-≥5 Root[] wiring (yoc follow-on)](064-solve-deg5-root.md) | 2026-05-07 | (unbeaded; logical follow-on of yoc) |
| 065 | [`tools/alg-num-arith` ships: wire envelope for `Root[poly, k]` field arithmetic](065-alg-num-arith-tool.md) | 2026-05-07 | (substrate for bead `iay`) |
| 066 | [`bench/alg-num-arith/` ships: cross-validate against SymPy `qqbar` (iay)](066-iay-bench-alg-num-arith.md) | 2026-05-07 | scientist-workbench-iay |
| 067 | [algNumInv `reverseCoefficients` term-order fix (5zh)](067-palindromic-minpoly-inv-fix.md) | 2026-05-07 | scientist-workbench-5zh |
| 068 | [ADR-0020: arbitrary-precision tier (bigfloat substrate; tstournament problem-13 forcing)](068-arbitrary-precision-tier.md) | 2026-05-07 | scientist-workbench-hv0 (epic), -hv0.1 (substrate; in-progress) |
| 069 | [`packages/bigfloat` + `tools/hypergeometric-pfq` shipped (hv0.1, hv0.3 closed)](069-bigfloat-and-pfq-shipped.md) | 2026-05-08 | scientist-workbench-hv0.1 (closed), -hv0.3 (closed); next: -hv0.5 |
| 070 | [`packages/meijer-core` Slater path + thin wire tool shipped (hv0.5 closed; bigfloat::exp regression filed)](070-meijer-core-slater.md) | 2026-05-08 | scientist-workbench-hv0.5 (closed), -4ne (P1 bug filed) |
| 071 | [`bigfloat::exp` "P1 regression" was a false alarm; principled hardening applied](071-bigfloat-exp-false-alarm-and-hardening.md) | 2026-05-08 | scientist-workbench-4ne (closed as false alarm) |
| 072 | [`packages/quadrature` arb-prec generalisation shipped (`gaussKronrodAdaptiveBF`)](072-quadrature-arbprec.md) | 2026-05-08 | scientist-workbench-hv0.7 (closed); ADR-0021 |
| 073 | [`packages/meijer-core` Mellin-Barnes contour layer + BigComplex G7K15 driver shipped (`hv0.8`)](073-meijer-contour.md) | 2026-05-08 | scientist-workbench-hv0.8 (closed); ADR-0022 |
| 074 | [`cas-core` special-function AST vocabulary extension shipped (`hv0.2`)](074-cas-core-special-functions.md) | 2026-05-08 | scientist-workbench-hv0.2 (closed); ADR-0023 |
| 075 | [Tanh-sinh quadrature WIP — driver shipped, smooth-analytic floor unresolved](075-tanh-sinh-wip.md) | 2026-05-08 | scientist-workbench-6f8 (claimed, **not closed**); ADR-0024 (partial) |
| 076 | [`meijer-core` Adamchik–Marichev symbolic dispatch shipped (`hv0.6`)](076-meijerg-symbolic-dispatch.md) | 2026-05-08 | scientist-workbench-hv0.6 (closed); ADR-0025 |
| 077 | [Tanh-sinh quadrature precision floor resolved — substrate-div integrand-contract bug](077-tanh-sinh-fixed.md) | 2026-05-08 | scientist-workbench-6f8 (resolved); ADR-0024 (shipped) |
| 078 | [`meijer-core` Braaksma asymptotic (Layer 6) shipped (`hv0.9`)](078-meijerg-asymptotic.md) | 2026-05-08 | scientist-workbench-hv0.9 (closed); ADR-0026 |
| 079 | [`bench/hypergeometric-pfq` tier-graded battery shipped (`hv0.4`)](079-bench-hypergeometric-pfq.md) | 2026-05-09 | scientist-workbench-hv0.4 (closed) |
| 080 | [`tools/meijer-g` top-level dispatcher (Layer 7) shipped (`hv0.10`)](080-meijerg-dispatcher.md) | 2026-05-09 | scientist-workbench-hv0.10 (closed); ADR-0027 |
| 081 | [`bench/meijer-g/` golden master battery shipped (`hv0.11`)](081-meijerg-bench.md) | 2026-05-09 | scientist-workbench-hv0.11 (closed) |
| 082 | [tstournament problem-13 staging shipped (`hv0.12`)](082-meijerg-tstournament-staging.md) | 2026-05-09 | scientist-workbench-hv0.12 (closed); campaign closed |
| 083 | [arbprec `--precision` flag wired through runner + compose (lc1 / rn2)](083-arbprec-precision-flag-wiring.md) | 2026-05-09 | scientist-workbench-{lc1, rn2} (closed); single source of truth in `mergedFlags` |
| 084 | [`bigfloat::div` precision-floor fix (`djp`) — substrate lifts the integrand contract from worklog 077](084-bigfloat-div-precision-floor-fix.md) | 2026-05-09 | scientist-workbench-djp (closed) |
| 085 | [Meijer G dispatcher coalescence fixes (`hv0.11.1`)](085-meijerg-coalescence-fixes.md) | 2026-05-09 | scientist-workbench-{7usr, fwsz} (closed); empirical precision estimator + ≥3-pole structured refusal |
| 086 | `bench/` → corpus migration COMPLETE: linalg trio (eigh + qr + svd) + ODE trio (ivp + stiff + symplectic) + real-root-isolate + poly-roots-radical + alg-num-arith + poly-factor-q + linsolve-q + solve + hypergeometric-pfq + meijer-g shipped — every workbench bench is now in the corpus; ~179 MB + ~4.3 MB + 1.3 MB other freed (ADR-0028, spup Pillar 2 closed); poly-roots-radical surfaced & fixed a canonicalisePolyTerms ascending-sort bug in packages/poly-factor; solve grades 94/100 with 6 pre-existing transcendental-lane drifts (3g9x; blocks b55); hypergeometric-pfq + meijer-g are arbprec-tier (platform_pinned=false; self-contained verify.ts on @workbench/bigfloat at 128 dps, no Python runtime) | 2026-05-10 | scientist-workbench-{uh2d, annr, dx0l, g6dn, ifng, yj80, 3pby, x18p, 5usl, 41yl, mnnm, upaz, 5gg5, gaih} (all closed; 3g9x open tracks the 6 transcendental-lane drift cases — blocks b55; si37 open tracks corpus schema extension for structured [meta.known_failures]); eigh 46/46, qr 56/56, svd 56/56, ivp 17/17, stiff 19/19, symplectic 17/17, real-root-isolate 37/37, poly-roots-radical 50/50, alg-num-arith 32/32, poly-factor-q 56/56, linsolve-q 46/46, solve 94/100 (rand-trans-{002,004,006,008,009,013} drift), hypergeometric-pfq 53/53, meijer-g 95/95 cases; meijer-g corpus@ecb7e05 |
| 087 | [Gröbner substrate + multivariate-poly `solve` lane (`x8d`)](087-groebner-substrate-and-multivariate-solve.md) — `packages/groebner/` (Buchberger 1965 + sloppy sugar (Giovini-Mora-Niesi-Robbiano-Traverso 1991) + Gebauer-Möller pruning in strict B/W93 §5.5 form + interreduction; FGLM order conversion (FGLM 1993); shape-lemma extraction (Becker-Mora-Marinari-Traverso 1994)) + `tools/groebner-basis/` (corpus bench `groebner-basis` 80/80 cases, 400/400 invariants) + `tools/solve` extended with the multivariate-poly lane via `solveGroebner`; ADR-0029 codifies the seven research-note decisions; `bench/solve` regrades 70/100 (24 mv-tier cases now correctly emit happy-path that the corpus verifier expects refusal — corpus-side follow-up filed as `ay4u`; 6 transcendental-drift unaffected = `3g9x` scope). | 2026-05-10 | scientist-workbench-x8d (Phase 3 ships); scientist-workbench-ay4u (filed for the corpus-side `multivariate-poly` lane in `bench/solve/golden/verify.ts` + reference) |
| 088 | [`solve` bench close-out: 100/100 cases, 354/354 invariants (`3g9x` + `ay4u`)](088-solve-bench-100-100-close-out.md) — Sub-shape D in `transcendental.ts`'s linear-arg decomposer (handles `head(c·varName − b)`); real-root count gate + alg-num `ROOT_VAR` rename in `groebner/shape-extract.ts`'s deg-2/3/4 path (closes complex-root leak through Cardano radicals); corpus-side `multivariate-poly` lane in `verify.ts` + reference's five-stage gate (lex GB → const-1 → Macaulay → shape-lemma → SymPy solve) mirroring `solveGroebner` bit-for-bit; verify.ts unary-after-binary `+`/`-` parser fix + `neg` head support + asymmetric grid window for transcendental completeness check.  Bench grade: 70 → 96 → 98 → 100/100. | 2026-05-10 | scientist-workbench-{3g9x, ay4u} (closed); -b55 (transcendental scope close-out, unblocked) |
| 089 | [LP bench onramp for Phase 1 (`eg9j`)](089-lp-bench-onramp.md) — Reading order + feedback-loop doc for an implementer landing on the convex-cone solver tier.  Phase 0 (the LP bench infrastructure in the corpus repo) closed; this shard is the bridge to Phase 1 (`cp9k` → `2ivi` / `wx3m`).  Names the v0.1 bench gate (21/21 lp-netlib + 29/29 lp-small, reframed from ADR-0030's nominal 98/114 + 110/114 pending v0.2 sparse wire format).  Phase-dependency chain documented. | 2026-05-11 | scientist-workbench-{1few, oz67} (closed -- corpus-side Phase 0 shipped); -{cp9k, 2ivi, wx3m, psuw} (Phase 1+ work ready to start; cp9k is the entry point) |
| 090 | [`tools/lp-solve` v0.1: arbprec rational engine (ADR-0031)](090-lp-solve-arbprec.md) — Exact-rational revised two-phase simplex over ℚ wrapped in the float64 wire (ADR-0030 §C/§D).  `packages/simplex-q` substrate: rat-cmp + product-form B⁻¹ + two-phase orchestrator with Bland-rule anti-cycling.  The killer claim: `achieved_precision ≈ ε_machine` on small dense, four orders past ADR-0030's 1e-12 ceiling, because the interior is exact ℚ and the only rounding is the wire encode.  Bench grade: 24/29 lp-small; netlib above scale ceiling (n ≥ 50 times out).  Original wx3m bead split into `wx3m` (arbprec, done today) + `hnyu` (float lane, future) + `prfp` (IPM lane, future).  Frictions: IEEE-754 sign-bit decode bug, row-negation dual sign-flip, exact vs ε-feasible float input. | 2026-05-11 | scientist-workbench-{wx3m, taui} (closed); -{hnyu, prfp} (registered as follow-ups) |
| 091 | [`@workbench/solver-ipm` substrate landing](091-solver-ipm-substrate-landing.md) — Phase 1 of landing a Mehrotra 1992 predictor-corrector primal-dual IPM substrate (LP + SDP, NT/AHO/HKM directions) alongside the same-day arbprec simplex ship.  Drops `packages/solver-ipm/` (17 src + 8 test files); substrate-only landing, no tool integration yet, simplex-q / lp-solve / ADR-0031 / worklog-090 untouched.  Adjustments: portable co-located `test/fixtures/sdp.dat-s`; `.ts` imports renamed to `.js` (workbench convention); 27 strict-null violations fixed (typed-array compound assignment under `noUncheckedIndexedAccess`).  Tests: 68/68 green.  Surfaced a pre-existing main-branch oracle failure on `lp-solve` golden 05 (filed as `2dhc`).  | 2026-05-11 | scientist-workbench-prfp (claimed, IPM lane dispatch); -{2zed, v4jd, 6or7, j1gd, 2dhc} (filed: ADR-0032, sdp-solve tool, contract hardening, algorithm hygiene, pre-existing golden bug) |
| 092 | [`tools/lp-solve` lane dispatcher (`prfp` closes)](092-lp-solve-lane-dispatcher.md) — Phase 2 of the parallel-agent merge.  Wires `@workbench/solver-ipm` into `tools/lp-solve` behind `--method=auto\|exact\|ipm` (default `auto`).  Free-variable splitting hoisted out of the engine call so both lanes see standard-form input.  Auto-dispatch threshold `m + splitN ≤ 50` → exact (bit-identical, world-first), else → IPM (NETLIB-scale).  Public wire schema unchanged; existing 12 goldens byte-identical (auto-dispatch routes all of them to exact).  Manual probe: `--method=ipm` on a 2-var LP returns `optimal` in 4 iters, `method: "solver-ipm"`.  Status taxonomy mapping: solver-ipm's `SolverStatus` collapses to ADR-0030 §A.3 wire taxonomy. | 2026-05-11 | scientist-workbench-prfp (closes) |
| 093 | [`solver-ipm` workbench-contract hardening (`6or7` closes)](093-solver-ipm-contract-hardening.md) — Phase 3 of the parallel-agent merge.  Substrate package hardened to workbench discipline: (1) NETLIB + lp-small tests rewritten from sweep-and-log to hard `expect()` assertions per case (Rule 7), `KNOWN_CONVERGENCE_GAPS = {brandy}` + `KNOWN_SUBSTRATE_GAPS = {H_malformed_cone, H_non_finite_input}` carved out with named-set comments tracking j1gd; (2) portable corpus path via `loadSuite()` (env `WORKBENCH_CORPUS` or sibling `../scientist-workbench-corpus`); (3) `--test` hook on `tools/lp-solve` exercising both lanes; (4) shared `toWireStatus` lifted to `packages/solver-ipm/src/solver/Status.ts` (single-source-of-truth between tool and tests), local `mapIpmStatus` in tool.ts deleted.  `numerical: true` + `--platform-fingerprint` already wired by the runner via `defineTool` annotation, no new code needed. 50/50 hardened tests pass with 142 `expect()` calls. | 2026-05-11 | scientist-workbench-6or7 (closes) |
| 094 | [`tools/sdp-solve` v0.1 + corpus `sdp-sdplib` bench (`v4jd` + `jkz6` close)](094-sdp-solve-and-corpus-bench.md) — Wraps `@workbench/solver-ipm`'s SDP IPM (NT primary, AHO A/B, HKM debug-only) into `tools/sdp-solve` behind ADR-0030 §C wire (PSDCone with strict-Mosek-format √2 off-diagonal scaling). v0.1 cone vocabulary: PSDCone + ZeroCone (NonNegCone deferred to v0.2 / bead `67nj`). 14 goldens, --test smoke hook, README. Corpus side: `benchmarks/sdp-sdplib/` shipped end-to-end with Mosek + COPT dual-witness oracle (Gurobi excluded — no SDP support; COPT runs in non-commercial size-limited free mode, n ≤ 2000 PSD dim covers all v0.1 cases with margin). 6 SDPLIB classics — control1/2/3, hinf2, theta1, mcp100. Tool grade: 3/6 cases, 63/66 invariants. The 3 failing cases (control2/3, hinf2) hit the substrate's SDP convergence gap — analog of LP NETLIB-`brandy`, filed as bead `qmrv`. Frictions: COPT writes license-banner to fd 1 from C library (fixed via `os.dup2` fd 1 → fd 2); COPT `getInfo(Dual, PsdConstraint)` returns wrong values (fixed via least-squares y reconstruction from KKT); `loadWorkbench()` cwd-walk fails when corpus runner spawns from non-workbench cwd (fixed by passing `toolsRoot` explicitly); `achieved_precision` must be computed in wire frame, not engine frame (svec scaling amplifies residuals). | 2026-05-11 | scientist-workbench-{v4jd, jkz6} (closes); -{qmrv, 67nj, tj6p} (registered/extended as follow-ups) |
| 095 | [solver-ipm verbose iter-trace + COPT-aligned termination → 5/6 sdp-sdplib](095-solver-ipm-verbose-trace-and-termination.md) — Two interlocking pieces on bead `qmrv`. (A) Unified verbose iter-trace pipeline: `VerboseIterLine` schema across LP + 3 SDP solvers (26 fields covering σ/μ_aff, αP/αD, 3-way reg state, Schur conditioning, eig proxies, phase timings); `formatVerboseLine` for stderr; opt-in JSONL mirror via `IPM_TRACE_JSONL`; `factorWith3Way` extended to return `{success, lastFailRow}`; four new scripts (`sdp-probe.ts`, `trace-diff.ts`, `copt-log-to-jsonl.ts`, `solver-ipm-bench.ts`); COPT 8.0.4 ground-truth captures committed under `docs/oracles/copt-sdpmethod0/`. (B) COPT-aligned termination + best-iterate (from `IPM_LOOP_CHEATSHEET §3` + decomp `FUN_00732a50` L482-620): 6-flag decision tree distinguishing OPTIMAL / DUAL_FEASIBLE / NUMERICAL_DIFFICULTY; verifier-aligned best-iterate snapshot (minimises `max(r_p/‖b‖∞, r_d/‖c‖∞, |cᵀx-bᵀy|/|cᵀx|, μ/|cᵀx|)`); `finalizeBestOr` helper routes all non-optimal exits through snapshot recovery; `Status.ts` maps `dual-feasible` → wire `optimal`. Bench grade: **3/6 → 5/6** (control2 + control3 pass). hinf2 holdout — `\|y\|_∞=567` makes `\|cᵀx-bᵀy\| = \|⟨S,X⟩ - yᵀ·r_p\|` inescapable without SDP-Ruiz equilibration. Frictions: instrumentation-before-fix is the load-bearing decision; wire-protocol vs dat-s-loader paths give different problem shapes; first pass missed Cholesky-failure exit (3 finalize→finalizeBestOr swaps); `\|A_i\|_F≈1` for hinf2 so naive row-equilibration is a no-op. | 2026-05-12 | scientist-workbench-qmrv (scope narrowed to hinf2 + SDP Ruiz; control2/3 closed inside) |
| 096 | [tools/{lp,sdp}-solve gain HSDE lanes + Phase 5 decomposed](096-tools-hsde-lanes-and-phase-5-plan.md) — Two-part wiring on bead `qmrv`. (A) Phase 3 of `docs/HANDOFF_solver_ipm_hsde_part2.md` ships: `tools/sdp-solve --method=hsde-nt` + `tools/lp-solve --method=hsde-lp`, defaults unchanged (HSDE without IR is no better than legacy on hinf2-class per the handoff). `HsdeSdpSolveResult` is a structural superset of `SdpSolveResult` so the existing encoder accepts it unchanged; `HsdeLpSolveResult` is flat (vs legacy nested `{status, iterate}`) so lp-solve gets a sibling `encodeHsdeLpResult`. Root README catalog rows refreshed (Law 2: stale "3/6" → "5/6" + HSDE-lane mention); demos 19, 20 in `scripts/demo-scope.ts` exercise all lanes through `wb.lpSolve` / `wb.sdpSolve`. Side-effect: 6 sdp-solve happy-path goldens regenerated — root cause is commit `6772307`'s additive `VerboseIterLine` emission perturbing float ordering (filed `dzmw`); confirmed pre-existing via `git stash`. (B) Phase 5 (iterative refinement → 6/6 hinf2) decomposed into 5 dependency-chained beads: `fuur` (ground-truth + diagnostic infra) → `vajd` (IR core) → `{fsr7, lniy}` (precision tests, corpus + default switch) → `rqbm` (worklog + handoff super). Tier 0 split out per the worklog-095 "build the loop before entering it" discipline. | 2026-05-12 | scientist-workbench-{tguv, 3wfr, dfez} (closed); -{9vc9, dzmw, fuur, vajd, fsr7, lniy, rqbm} (filed) |
| 097 | [sdp-solve infeasibility classification (`io2v` closes)](097-sdp-infeasibility-classification.md) — Surfaced by `jb1x`'s SDPLIB stress test: the 4 explicitly-infeasible cases (infp1/infp2 primal-infeas, infd1/infd2 dual-infeas) returned wire `status=optimal` with `ap` ∈ [1e+1, 1e+5]. An honest-scope violation (Rule 8) caused by two distinct bugs. (1) `NtSdpSolver.ts`: best-iterate stamp `bestStatus = "dual-feasible"` set unconditionally on any `achieved` improvement; Status.ts maps `dual-feasible → optimal` (worklog 095) ⇒ infeasible inputs return wire `optimal` with the huge residuals from iter-0 snapshot. Fix: gate stamp on `couldDualFeas` (a flag the code already computed and `void`'d). (2) `HsdeNtSdpSolver.ts`: ADR-0033 Decision 6 gated *both* OPTIMAL and INFEASIBLE classifications on `max(ρ_p, ρ_d, ρ_g) ≤ 1`, but the purified ρ-metrics inflate as τ→0 (exactly the infeasibility-certificate regime), so the gate never trips on infeasible inputs. Fix: split — OPTIMAL keeps purified ρ-gate; INFEASIBILITY uses *unpurified* witness-ratio tests (`b^T y > 0` with small `‖r_d‖`; `⟨C, X⟩ < 0` with small `‖r_p‖`), mirroring Mosek `hom_terminatelo` (decomp `003f8460`). Witness/collapse floors (`WITNESS_FLOOR = 1e-6`, `TAU_KAPPA_FLOOR = 1e-8`) guard against degenerate τ+κ→0 and almost-converged-optimal mis-fires. 4 minimal hand-constructed SDPs in `packages/solver-ipm/test/sdp-infeasibility.test.ts` (8 tests, all pass). HSDE-NT now classifies primal-/dual-infeasibility at iter=2 on the probes with τ≈1e-12, κ≈1. NT now returns honest `numerical-error` on infeasible inputs (never lies "optimal"). ADR-0033 §6/§7 updated. | 2026-05-12 | scientist-workbench-io2v (closes) |
| 098 | [`@workbench/qinfo` substrate + tensor-product + partial-trace tools (`eq4a` + `qn98` + `4jux` close)](098-qinfo-substrate-and-first-tools.md) — Open the qinfo epic (`hsxa`, dogfood-driven 2026-05-10). Substrate package `packages/qinfo` ships with `Matrix = {rows, cols, re, im?}` (row-major Float64Array, optional imaginary part), `kron`, `partialTrace`, `partialTracePure` (Schmidt-decomposition O(d·rank) path for state vectors), `partialTranspose`, `vec`/`unvec`, `choi`/`deChoi`. **Three principle-led re-scopings** beyond `eq4a`'s original spec: dims-generalised (arbitrary `[d_0, d_1, …]` not 2^n-only); flat Float64Array storage with optional `im` (not nested `number[][]`); complex from day 1 on the index-only ops (the original real-only restriction applied only to eigh-routed paths, not pure index manipulation). 56 substrate tests / 87 expect calls, every test asserts a mathematical invariant — Rule 7 verbatim (mixed-product law `(A⊗B)(C⊗D)=(AC)⊗(BD)`, product-state `Tr_1(A⊗B)=tr(B)·A`, Bell ρ_A=I/2, vec identity `vec(AXB^T)=(B⊗A)vec(X)`, J(id)=`|Ω⟩⟨Ω|`, PT involution, choi round-trip). Two v0.1 tools — `tools/tensor-product` (11 goldens) and `tools/partial-trace` (12 goldens with dims-generalised wire) — both pass `--test` and oracle. Typed barrel auto-extended: `wb.tensorProduct(...)` and `wb.partialTrace(...)` are first-class. Two new tool beads filed: `yjs9` (partial-transpose, tool wrapper) and `pk0c` (choi-iso). ADR-0034 documents 8 decisions. `bun run check`: 78 passed / 7 skipped / 1 failed (pre-existing corpus path). Hastings-style structured fast paths deferred to v0.3 (need structured-operator types). | 2026-05-12 | scientist-workbench-{eq4a, qn98, 4jux} (close); -{yjs9, pk0c} (filed) |
| 099 | [qinfo v0.1 complete (choi-iso + partial-transpose) + canonicalize typed switch (`pk0c` + `yjs9` + `80i` close)](099-qinfo-v01-complete-and-canonicalize-typed.md) — Ship the two qinfo follow-up tools filed in 098: `tools/choi-iso` (forward + inverse Choi–Jamiołkowski isomorphism, **discriminated-union input** via `S.union([S.tagged("channel-to-choi", …), S.tagged("choi-to-channel", …)])` — first input-side use of the protocol vocabulary; matches Watrous / QuTiP `to_choi` / Qiskit `Choi` / Wood–Biamonte–Cory Eq. 3.22 column convention verified by Sonnet research subagent), `tools/partial-transpose` (PT_S on arbitrary subset; involution + whole-system-PT-equals-transpose + Bell-state-PPT-witness all in `--test`). 12 goldens each. Two new demo-scope sections (`#21 choi-iso ∘ linalg-eigh` exposes `J(transpose) = SWAP` with eigenvalues `{-1, 1, 1, 1}`; `#22 partial-transpose ∘ linalg-eigh` exposes the Peres-Horodecki `-1/2` witness on the Bell state) — same criterion from channel-side and state-side. Small refactor in the same session: `packages/protocol/src/canonical.ts` rewritten as exhaustive switch on `Value.kind`; `noFallthroughCasesInSwitch` enforces coverage; `validateAndCanonicalize` companion dropped (no caller has unknown input). Bead-text-vs-precedent: both wrappers refuse via `ToolError` rather than the bead-suggested tagged categories (math has no out-of-scope branch — same call as partial-trace). All three checks (post-each-tool + post-canonicalize) green; protocol round-trip 1000-trial property tests + 350+ goldens unchanged byte-identical. | 2026-05-12 | scientist-workbench-{pk0c, yjs9, 80i} (close) |
| 100 | [Complex-Hermitian linalg tier: ADR-0035 (`q4f9`)](100-complex-linalg-tier-adr.md) — Resolve the open decision in `ov4j` by writing ADR-0035. Eight decisions, all additive: (D1) parallel `*-complex` tools mirroring the `meijer-g-*` fan-out (rejected extend-existing-tool and bridge-package options); (D2) wire shape `record{re, im}` with `im` **required** and shape-matched to `re` (rejected per-cell complex as 3-4× wire inflation, optional-`im` as a runtime-branch surface); (D3) `numerical: true` tier; Hermitian eigenvalues stay `list<float64>`, eigenvectors are complex `record{re, im}`; (D4) new `ComplexMatrix` type in `@workbench/linalg-core` structurally identical to qinfo's `Matrix`-with-im but with `im` required for type discipline (bridge helpers `complexFromReal` / `complexFromQinfo` / `realPartOnly`); (D5) algorithm v0.1: real-symplectic embedding `H = A + iB → [[A,-B],[B,A]]` reuses existing real cyclic-Jacobi `eigh` in full (~120 LOC wrapper, zero new spectral code); (D6) Hermiticity boundary tag + non-finite / degenerate-shape parallel to real eigh; (D7) output schema parallel to `linalg-eigh` with complex `Q` and real eigenvalues; (D8) phase order: eigh-complex first (unblocks `korg` trace-norm + qinfo v0.2 surface), then svd-complex, then solve-complex. Lockstep doc edits: `CLAUDE.md` hallucination-risk callout, `README.md` value-protocol paragraph. Three downstream beads (`linalg-eigh-complex`, `linalg-svd-complex`, `linalg-solve-complex`) deferred to a next-session filing. | 2026-05-13 | scientist-workbench-q4f9 (closes); -ov4j (epic, ADR resolves the open decision) |
| 101 | [linalg-eigh-complex: ADR-0035 phase 1 ships (`pom8`)](101-linalg-eigh-complex.md) — Ship `tools/linalg-eigh-complex` end-to-end. Substrate side: `packages/linalg-core/src/complex-matrix.ts` (the `ComplexMatrix` type + the three ADR-0035 bridge helpers `complexFromReal` / `complexFromQinfo` / `realPartOnly` + elementary ops `complexAdjoint` / `complexFrobeniusNorm` / `complexMaxAbs` / `complexMatmul`) and `eigh-complex.ts` (the real-symplectic embedding wrapper — builds `H̃ = [[A,-B],[B,A]]`, calls the existing real `eigh` verbatim, takes every-other eigenvalue + lifts the top-and-bottom halves into `u + iw`, then runs complex Modified Gram-Schmidt as defence-in-depth for degenerate eigenspaces). Tool side: full seven-artefact contract — `tool.ts` with Hermiticity check, boundary tags (`non-hermitian-input` / `non-finite-input` / `degenerate-shape`), wire encode/decode for the required-`im` `record{re, im}` shape, `--test` hook with 4 invariant-asserting probes (Pauli Y/Z, generic Hermitian, 4×4 random); 34 goldens covering Pauli family, density operators (real + complex Bloch with Y component — the dogfood target), 3-5x5 sizes, multi-qubit Hamiltonians (Z⊗Z, X⊗Y, Bell density), degenerate spectra (exercises the MGS pass), and every boundary branch. Lockstep: tool-catalog row + linalg-core package-description update in root `README.md`; typed barrel regenerated (now 47 tools, `wb.linalgEighComplex`). Pauli Y smoke check inline-tested before commit: eigenvalues exactly `±1`, reconstruction `0`, orthogonality `~3e-16`. Unblocks `korg` (trace-norm) and the rest of the qinfo v0.2 quartet. | 2026-05-13 | scientist-workbench-pom8 (closes); -ov4j (epic, phase 1 of 3) |
| 102 | [trace-norm: the first qinfo v0.2 tool (`korg`)](102-trace-norm.md) — Ship `tools/trace-norm`, the first deliverable of the qinfo v0.2 surface (unblocked by shard 101's `linalg-eigh-complex`). The Schatten-1 norm `‖M‖₁` of a complex Hermitian matrix via the spectral characterisation `‖M‖₁ = Σ |λ_k|` (Bhatia §IV.2): ~30 lines of tool body over the substrate. Wire input `record{M: record{re, im}}` (the matrix wrapped in a record so the surface can grow additively); output `record{value, eigenvalues, condition_number, method, warnings}` — pass-through diagnostics from the eigh substrate cost zero extra and give downstream planners useful spectral gates (purity is `Σ λ²` computed client-side, trace-distance bounds gate on the spectrum). Hermiticity check at `100·EPS·max|M|`; non-Hermitian refuses via `trace-norm/non-hermitian-input` rather than silently routing to SVD (the SVD path is ADR-0035 phase 2, filed under `ov4j`). 35 goldens covering identity sizes, pure projectors, max-mixed density operators, the Pauli family, Bloch states (real + complex Y-component dogfood target), generic Hermitian, mixed-sign diagonals, multi-qubit Hamiltonians, degenerate spectra, well-separated extremes, and every boundary branch. `--test` hook with 8 invariant-asserting probes. Demo-scope #23 exercises the tool through the typed barrel, producing the textbook Helstrom trace-distance values `T(|0⟩⟨0|, |1⟩⟨1|) = 1` and `T(|0⟩⟨0|, I/2) = ½` to 1e-12. Typed barrel regenerated (48 tools, `wb.traceNorm`). | 2026-05-13 | scientist-workbench-korg (closes); -ov4j (epic; phase 1 → first downstream tool ships) |
| 103 | [purity: the second qinfo v0.2 tool (`2czd`)](103-purity.md) — Ship `tools/purity`, the second deliverable of the qinfo v0.2 surface (sibling to `trace-norm`). Purity `γ(ρ) = tr(ρ²)` of a complex Hermitian density operator, computed via the entrywise sum-of-squares identity `γ = Σ_{i,j} |ρ_{ij}|² = Σ (re² + im²)` that follows from Hermiticity — **no eigendecomposition needed**, O(n²) instead of trace-norm's O(n³). Wire input lifted from the bead's pre-trace-norm `list<list<float64>>` to the consistent `record{rho: record{re, im}}` shape so the qinfo v0.2 quartet has one wire convention for the matrix; output `record{value, trace, is_pure_within_tolerance, method, warnings}` — the named flag answers "is ρ pure?" at a documented tolerance, `trace` is a free byproduct surfaced for caller-side density-matrix validation. Hermiticity check at `100·EPS·max|ρ|` (same threshold as trace-norm + eigh-complex); non-Hermitian refuses because γ of non-Hermitian is not real and not bounded by `[1/d, 1]`. Three soft-warning branches (`tr ≠ 1`, `γ > 1`, `γ < 1/d`) honestly report "input may not be a density operator" without refusing — a user computing γ(Pauli X) gets `2` with a warning rather than a refusal. 34 goldens covering pure projectors (real + complex-Hermitian Pauli-Y eigenstates), max-mixed at d ∈ {2,3,4}, rank-2 mixtures sweeping [1/d, 1], Bloch states, Bell + Ψ⁻ + product two-qubit states, separable classical mix, Pauli observables (γ = 2 with warnings), unnormalised identity, the zero operator, generic complex Hermitian, 5×5 stress, and every boundary branch. `--test` hook with 10 invariant probes including a unitary-invariance cross-check (rotate diag(0.7, 0.3) by 60°, γ unchanged) and the spectral-identity sweep `γ = p² + (1−p)²` for p ∈ {0.1, 0.3, 0.5, 0.7, 0.9}. Demo-scope #24 exercises the canonical entanglement signature: γ(|Φ+⟩⟨Φ+|) = 1, γ(tr_B |Φ+⟩⟨Φ+|) = 1/2 — the drop is the entanglement, state-side companion to demo #22's Peres witness. Typed barrel regenerated (49 tools, `wb.purity`). | 2026-05-13 | scientist-workbench-2czd (closes); -hsxa (qinfo epic; second of four v0.2 tools) |
| 104 | [trace-distance: the third qinfo v0.2 tool (`k2xo`)](104-trace-distance.md) — Ship `tools/trace-distance`, third deliverable of the qinfo v0.2 surface (sibling to `trace-norm` and `purity`). Trace distance `D(ρ, σ) = ½‖ρ − σ‖₁` between two complex Hermitian density operators, the operationally-meaningful Helstrom distinguishability metric (P_distinguish = (1+D)/2). Same `eighComplex` substrate as `trace-norm` applied to the Hermitian difference `ρ − σ` — spectral characterisation `D = ½ Σ |λ_k(ρ−σ)|` (Bhatia §IV.2 + halving factor). Wire input `record{rho: record{re, im}, sigma: record{re, im}}` matches the v0.2 quartet convention; output `record{value, method, warnings}` is deliberately minimal (eigenvalues of `ρ−σ` aren't surfaced because they're a derived intermediate — call trace-norm on the difference if needed). New boundary class: `trace-distance/shape-mismatch` for `n_ρ ≠ n_σ` (operators on different Hilbert spaces — honest refusal). All other boundary tags (`non-hermitian-input`, `non-finite-input`, `degenerate-shape`) carry a `which` field ∈ {"rho", "sigma"} so the planner can locate the bad input without re-decoding. 29 goldens covering orthogonal-pure-states-saturate at d=2,3 (computational, X-eigenbasis, complex Y-eigenbasis), identity-of-indiscernibles, pure-vs-max-mixed at d∈{2,3,4} (D=1−1/d), Bell projector pairs (Φ+ vs Φ−, Φ+ vs Ψ+), flipped classical bits, Bloch geometric (D=|Δr|/2 including the complex Y-flip dogfood case), symmetry probe (input swap), triangle probes (three diagonal mixtures saturating D(a,c)=D(a,b)+D(b,c)=0.6), and every boundary branch. `--test` hook with 7 invariant probes including explicit symmetry and triangle assertions. Demo-scope #25 demonstrates Helstrom perfect-distinguishability `D=1 ⇒ P_distinguish=1` plus the triangle inequality saturated on three collinear-Bloch states (`D(a,b)+D(b,c) = 0.6000 ≥ D(a,c) = 0.6000`). Decoder/Hermitian-check helpers now triplicated across the trio — lift to a shared substrate deferred until fidelity's two-matrix pattern lands, then designed against the four-tool union. Typed barrel regenerated (50 tools, `wb.traceDistance`). | 2026-05-13 | scientist-workbench-k2xo (closes); -hsxa (qinfo epic; third of four v0.2 tools) |
| 105 | [fidelity: the qinfo v0.2 trio complete (`2hxf`)](105-fidelity.md) — Ship `tools/fidelity`, fourth and final qinfo v0.2 tool. Uhlmann fidelity `F(ρ, σ) = (tr √(√ρ · σ · √ρ))²` via the spectral path: three `eighComplex` calls (for `√ρ`, then for the inner `√ρ · σ · √ρ`, plus the implicit Hermitian-PSD square root) and two `complexMatmul`s, all `O(n³)`. Output surfaces three byproducts of the inner computation: `value` (F itself), `sqrt_value` (`√F`, the Bhattacharyya overlap — input to Fuchs–van de Graaf), and `bures_angle` = `arccos(min(1, √F))` (Riemannian geodesic distance under the Bures metric, ∈ [0, π/2] radians; the `min(1, ·)` guards against floating-point `√F = 1 + 1e-16` producing `NaN` from `arccos`). PSD violation on either input is a soft warning (negative eigenspaces clamped to 0 in the square-root construction, then again in the trace-of-sqrt), not a refusal — agent-honest computation on near-PSD inputs from noisy-channel outputs. The Hermitian-PSD spectral-sqrt is implemented inline (`hermitianPSDSqrt(eighResult, …)`) rather than lifted to `@workbench/linalg-core` as new substrate — YAGNI; one caller v0.1, lift when a second emerges. 28 goldens covering identical states across shapes (F=1 including identical Bell), orthogonal pure-states (F=0) in computational/X/Y eigenbases, pure-pure overlap `|⟨ψ|φ⟩|²` at fixed angles, pure-vs-max-mixed at d ∈ {2, 3, 4} (F=1/d), flipped classical bits (closed-form `(√(pq) + √((1−p)(1−q)))²`), Bloch X-Z pairs, complex Y-sign-flip dogfood, symmetry probe, classical-vs-pure (F=p), every boundary branch. `--test` hook with 9 invariant probes including symmetry F(A,B)=F(B,A) and **three Fuchs–van de Graaf probes** (`1 − √F ≤ D ≤ √(1 − F)`) on independent state pairs. Demo-scope #26 prints both D and F on two probes side-by-side: probe 1 (pure-vs-max-mixed) shows the inequality with bounds far apart (`0.2929 ≤ 0.5000 ≤ 0.7071`); probe 2 (generic mixed pair) shows the upper bound near-tight (`D = 0.3162 ≤ √(1−F) = 0.3169`, agreement to three decimals). Decoder/Hermitian-check now **quadruplicated**; lift to shared `wire-decode.ts` filed as the immediate follow-up. Typed barrel regenerated (51 tools, `wb.fidelity`). The qinfo v0.2 quartet (`trace-norm`, `purity`, `trace-distance`, `fidelity`) is **complete** as of this session. | 2026-05-13 | scientist-workbench-2hxf (closes); -hsxa (qinfo epic; fourth of four v0.2 tools — quartet complete) |
| 106 | [HSDE Phase 5 Tier 0: ground truth + diagnostic slots](106-hsde-phase5-tier0-diagnostics.md) | 2026-05-13 | scientist-workbench-fuur |
| 107 | [Trace-schema unification: one `TraceLine`, typechecked parsers](107-trace-schema-unification.md) — Reconcile the three hand-maintained verbose-trace interfaces (`VerboseIterLine`, `CoptIterLine`, `MosekIterLine`) into one typechecked schema. New `packages/solver-ipm/src/solver/TraceLog.ts` defines `TraceLine` — the persisted-JSONL superset (six solver kinds plus `"copt"`/`"mosek"`, every solver-internal field `number \| null`) — with a compile-time assertion proving every solver-emitted `VerboseIterLine` is a valid `TraceLine`, so schema drift is a build error. `"mosek"` removed from `VerboseIterLine.kind` (it never belonged in the solver-emitted type; Tier 0 added it inconsistently — `"copt"` was never added). COPT/Mosek log-parsing logic lifted from the untyped scripts into `parseCoptLog`/`parseMosekLog` in the package (where `tsc` checks it — `scripts/` is outside the `include`); `scripts/{copt,mosek}-log-to-jsonl.ts` shrink to ~40-line CLI shells. New `trace-log.test.ts` with 11 parser-behaviour probes. ADR-0033 Decision 8 rewritten for the two-type split. No solver trajectory change. | 2026-05-14 | scientist-workbench-ghvl (closes); follow-up to `ef3a56c` review (`yyme`, `vvou` remain) |
| 108 | [Mosek/COPT trace-parser verification against real logs](108-mosek-copt-trace-parser-verification.md) — Close `yyme`: `parseMosekLog` had only ever been validated against a synthetic file hand-written to match its own assumptions (a tautology — `fuur`'s acceptance criterion 4, a real `probe_sdo1.log`, was never met). With Mosek 11.1.6 and COPT 8.0.4 confirmed on the machine, ran both solvers' interior-point optimizers on NETLIB `adlittle` and verified `parseMosekLog`/`parseCoptLog` field-by-field against the real output. Both format assumptions were already **correct** — the finding was "unverified", not "wrong", so no parser code changed. Committed the real logs as `test/fixtures/{mosek-11.1,copt-8.0.4}-adlittle.log`; added a second `trace-log.test.ts` layer (6 probes, 11→17 tests) asserting iteration ranges + column-by-column values transcribed from the raw rows. Cleared the `!! FORMAT NOT YET VERIFIED` caveat in `TraceLog.ts` and the matching caveat in ADR-0033 Decision 8; fixed the COPT comment's stale `probe1.log` reference (never committed) to the real fixture. Gurobi 13.0.1 also present — no parser exists, left unfiled as net-new scope. | 2026-05-14 | scientist-workbench-yyme (closes); `vvou` remains from `ef3a56c` review |
| 109 | [`parseGurobiLog`: the third external-solver trace witness](109-gurobi-trace-parser.md) — Close `z799`: add `parseGurobiLog` alongside the COPT/Mosek parsers so `trace-diff.ts` has a third independent reference (a TS trace can then be judged "matches two of three", not just one). Gurobi's barrier log has a *distinct* format — seven columns `Iter Primal Dual Primal Dual Compl Time` under a two-line header, with `Compl` in position 5 vs COPT's position 3, so a COPT alias would silently mis-map. Verified field-by-field against a real Gurobi 13.0.1 `Method=2` solve of NETLIB `adlittle` (committed as `test/fixtures/gurobi-13.0-adlittle.log`); all three solvers converge to obj ≈ 2.254950e+05. New `scripts/gurobi-log-to-jsonl.ts` thin CLI shell; `"gurobi"` added to `TraceLine.kind`; 8 new `trace-log.test.ts` probes (17→25); ADR-0033 Decision 8 updated. Also closes `vvou` (sibling `ef3a56c` finding): the three corpus tests that `throw` on missing corpus converted to the `describe.skip` convention of `netlib.test.ts`/`lp-small.test.ts` — verified corpus-missing → skip not fail. The `ef3a56c` review is now fully discharged. | 2026-05-14 | scientist-workbench-z799 (closes); -vvou (closes) |
| 130 | [HSDE Phase 5 Tier 4: docs-lockstep close-out](130-hsde-phase5-tier4-docs-lockstep.md) — Close `rqbm` (Tier 4) + `qmrv` (parent bug). Admin-only: three predecessor handoffs (`HANDOFF_solver_ipm_hsde.md`, `HANDOFF_solver_ipm_hsde_part2.md`, `HANDOFF_solver_ipm_qmrv.md`) gain SUPERSEDED headers pointing at the per-tier worklogs (106 / 110 / 128 / 129) that landed the work. **Bead `rqbm` anticipated worklog 097** — that slot was occupied (sdp-infeasibility-classification, 2026-05-14) before Phase 5 started; the four Phase 5 tiers each landed their own worklog. Tier 4 reinterpreted as: write this short close-out shard (130) + refresh the doc surface. **Root README sdp-solve catalog row + tools/sdp-solve/README.md lane table** both updated: `--method=auto` now described as routing to HSDE-NT (was: legacy NT), the "A/B-grade today pending Phase 5 IR" qualifier on the HSDE lane is dropped (Phase 5 has shipped), soft-success branch named inline, bench grade **5/6 cases, 64/66 invariants** with the per-failure breakdown (`hinf2 primal_feasibility + complementary_slackness` only — the Phase 6 gate). **Bead `qmrv` closes with the honest outcome:** structurally resolved αP-collapse via HSDE's homogenization; lifted invariant count 63→64; remaining 2-invariant hinf2 gap is the float64 representation of `r_p / τ` purification, lifted by Phase 6 bigfloat (separate ADR per ADR-0033 Decision 9). `ki4c` (TOL_KKT tightening) stays open — float64 floor verdict argues against tightening for now. Supersession headers preserve the original handoffs below the blockquotes (each carries 300-700 LOC of design reasoning earned during implementation; deleting them would leave the worklogs as the only record of "why we did it this way"). Handoff `qmrv` specifically named as a CLAUDE.md Rule 2 cautionary tale: the Ruiz fix it proposed addressed a symptom (`|y|` large) without investigating the cause, would have been 200 LOC that didn't help. No code changes; typecheck + tests unchanged. | 2026-05-16 | scientist-workbench-{rqbm,qmrv} (closes both); -ki4c (left open) |
| 129 | [HSDE Phase 5 Tier 3: --method=auto → hsde-nt, soft-success bridge](129-hsde-phase5-tier3-default-switch.md) — Close `lniy` (and `y3qd` as superseded): switch `tools/sdp-solve --method=auto` default from legacy NT to HSDE+IR, add a soft-success classification to HSDE NT termination so the corpus `status_consistency` check accepts ill-conditioned cases (parallel to legacy NT's `couldDualFeas` branch). **Soft criterion (in `HsdeNtSdpSolver` snapshot logic):** `μ ≤ feasTol AND prstatus > 0.5 AND τ ≥ 1e-6`, classifies as `dual-feasible` (`Status.ts` wire-maps to `optimal`; agent sees `status="optimal"` + `achieved_precision` carrying the true purified ρ-max). Deliberately NOT a `ρ_p`-bound check — the float64 floor is in `r_p / τ` purification, not the back-sub, so a ρ-bound `K` is arbitrary; the absolute μ-test is the principled inheritance from legacy NT's COPT-aligned 6-flag tree. **First draft used `ρ-band, K=10` sized to corpus-tolerance/agent-precision ratio — caught `control3` but missed `hinf2` because hinf2's late-iter ρ_p > 10 even at the best snapshot. μ-test catches both; the lesson is that ρ-bands are non-stationary, μ is stationary.** `tools/sdp-solve --method=auto` now returns `method=solver-ipm-hsde-nt iter=4 status=optimal`; `--method=nt` preserved for back-compat. 6 PSD-success goldens regenerated via `generate-goldens --tool sdp-solve` (5-char method-tag prefix + iter-count + ULP drift); 8 refusal goldens byte-identical. ADR-0033 §"Decision 9 — Tier-3 amendment" records the verdict. **Corpus bench delta:** baseline 5/6 (63/66 invariants) → Tier 3 5/6 (64/66) — `hinf2` `optimality_gap` flips to pass (IR's 3-decade `r_p` improvement); `control3` lifts onto HSDE+IR's soft-success path. Case-count 6/6 remains a Phase 6 gate per Tier 2 (`hinf2` purified `pInf ≈ 2e-6` on the svec-scaled wire encoding exceeds the verifier's `1e-7 · max(1, ‖b‖_∞)` tolerance regardless of termination class). **Wire encoding vs direct `.dat-s` loading produce different trajectories** (svec √2 off-diagonal scaling changes the Schur structure); direct probe takes 133 iters with `pInf = 5.64e-8`, corpus path takes 26 iters with `pInf = 2e-6`. Both valid; worth knowing for future diff-vs-direct work. `bun tools/sdp-solve/tool.ts --test` passes; oracle 14/14 on goldens. 117 pass / 2 skip / 0 fail / 3 errors (pre-existing fixture gap, bead `n59x`). | 2026-05-16 | scientist-workbench-{lniy,y3qd} (closes both) |
| 128 | [HSDE Phase 5 Tier 2: Mosek oracle, precision-floor verdict](128-hsde-phase5-tier2-precision-floor.md) — Close `fsr7`: the Mosek-comparison oracle + end-to-end precision tests for `solveHsdeSdpNt`. Mosek 11 dropped SDPA-sparse from `mosek.dataformat`, so the bridge is a Python script (`scripts/oracles/mosek-sdpa-probe.py`, ~210 LOC) that parses SDPA inline and feeds `mosek.Task` via `appendbarvars` + `appendsparsesymmat` + `putbaraij`/`putbarcj`; SDPA-MAX `b^Ty` ↔ Mosek-MIN `⟨C, X⟩` is the natural dual pairing. Captured 6 logs + JSONL conversions under `docs/oracles/mosek-sdo/` for control1/2/3, hinf2, theta1, mcp100; `parseMosekLog` (worklog 108) worked unchanged on the conic-IPM iter table. **Test surface** (`packages/solver-ipm/test/hsde-precision.test.ts` describe "HSDE NT — end-to-end precision on SDPLIB corpus (Tier 2)"): 8 case-tests + corpus-missing skip; control1/control2/theta1/mcp100 reach strict `optimal` at default tol; control3 reaches the float64 floor (objective correct to 1e-3, `pInf ≤ 1e-7`); hinf2 reaches `pInf ≤ 1e-7` (float64 floor after IR); strict 1e-9 hinf2 + strict optimal control3 are `test.skip("[PHASE 6 GATE — bigfloat required]")`. 14 pass / 2 skip / 0 fail / 27 expects. **Diagnosis (single-run, `temp/probe-hinf2-tune.ts`):** IR has bought the full 3 decades on unpurified `r_p` (Tier 0 `1.26e-7` → Tier 1 `1.77e-10`); the 2-decade purified-`pInf` gap from the 1e-9 target is **entirely τ-shrinkage** — `τ` collapses to `~3e-3` by hinf2's best iter, exactly cancelling IR's gain on `pInf_purified = r_p / τ`. No tuning of `LINSYSACC`, `IRERRFACT`, or `maxIter` in `solveWithIR` can recover the missing decades — the limit is the purification step, not the back-substitution. Same diagnosis on control3 (best `pInf_purified = 5.85e-8` vs `~2e-8` threshold; `rhoP = 2.92`). The bead's honest-scope clause is discharged: **Phase 6 (bigfloat HSDE, separate ADR per Decision 9) is the only path past the float64 floor on these two cases.** ADR-0033 §"Decision 9 — Tier-2 amendment" records the per-case verdict + Phase 6 gate. `scripts/sdp-probe.ts` extended with `--method=hsde-nt` (the bead's end-to-end diff command needs it; uniform `summarise()` across solver kinds so the stdout line is composable). `test/corpus.ts` gains `loadSdplibRaw(caseId)` (skip-not-fail discipline, mirrors `loadSuite`). 3 pre-existing errors in `trace-log.test.ts` (missing fixtures named by worklog 108) filed as bead `n59x` (P2 bug). | 2026-05-16 | scientist-workbench-fsr7 (closes); -n59x (filed); -lniy (unblocked) |
| 127 | [linalg-svd-complex: complex SVD via one-sided Jacobi (ov4j phase 2)](127-linalg-svd-complex.md) — Close `jao0`: ship `tools/linalg-svd-complex` (the complex SVD parallel to `linalg-svd`). **Algorithm: complex one-sided Jacobi** (Hari-Veselić 1987), NOT the real-symplectic embedding — ADR-0035 §D8 says the embedding "buys nothing for SVD" because SVD's distinct U / V structure breaks the eigenvalue-pairing trick that makes `linalg-eigh-complex` viable. Per-pair update: extract phase `e^{-iθ}=conj(γ)/|γ|` to make Gram inner product real, then standard real Jacobi tangent recipe `(c, s)`, apply combined complex rotation to W and V accumulator. Convergence: monotone off-diagonal mass decrease (Hari-Veselić §3). Drmač 1997 §4.2 per-pair relative tolerance test preserves Demmel-Veselić relative accuracy on small singular values. Bead-description algorithm mismatch caught at ADR re-read (Law 1), before any code — first draft had proposed embedding by analogy with eigh-complex. **Substrate** (`packages/linalg-core/src/svd-complex.ts`, ~570 LOC including diagnostics + complete-mode complex Gram-Schmidt) emits `V` directly; the **tool** adjoints once for the wire emission of `Vh = V†` (NumPy convention, parallel to real `Vt`). Handles m<n via internal M† routing + (U, V) swap at the end. Reduced + complete modes; agent-honest reconstruction + per-factor unitarity errors + LAPACK rank threshold. 35 goldens covering Pauli fixtures, Hermitian (cross-checked vs `eighComplex`), rectangular both directions, complete mode, rank-deficient, boundary tags. **Trace-norm extended** with two-path dispatch: Hermitian → `eighComplex`+|λ|, non-Hermitian → `svdComplex`+ΣS — the legacy `trace-norm/non-hermitian-input` refusal class is retired; 3 previously-refused goldens regenerate as successes with new optional `singular_values` field and `method="general-via-svd-complex"`. **Frictions**: bead-ADR algorithm mismatch caught early; one test bug (LAPACK rank threshold misexpectation, fixed by reading the threshold formula); `bd dep add` syntax discovered via `--help`. `bun test packages/linalg-core/ tools/linalg-svd-complex/ tools/linalg-eigh-complex/ tools/trace-norm/ tools/trace-distance/ tools/fidelity/ tools/purity/`: 146 pass / 0 fail / 1250 expect() calls. | 2026-05-16 | scientist-workbench-jao0 (closes) |
| 126 | [atip: the δπ algebraic-sector envelope (narrowed to κ ≥ 3)](126-atip-degenerate-principal-sector.md) — Close `atip`: the P1 bug surfaced by `ulze` (worklog 125) where κ=3 δ=0 shapes (deleted golden 17 `G^{1,1}_{1,3}`) silently emitted answers wrong by ~125× AND wrong sign. **Bead's "refuse all δ≤0" was narrowed during implementation to "κ ≥ 3 AND δ ≤ 0"** after the first implementation pass broke 7 shipped mpmath-verified bead-43i tests at κ=1 δ=0 (the `G^{1,1}_{2,2}`/`G^{1,2}_{3,3}`/`G^{1,3}_{4,4}` family). Math story: for κ=1 the inner pFq is `pFp-1(1/z)` with radius 1, converges for \|z\|>1, and Slater 1966 §5.5's q≥p theorem makes H = G as a *convergent* formula — δ has no role. For κ≥3 the inner is `qFp-1(1/z)` with q>p−1 lower, formally divergent, and the algebraic envelope `\|arg z\| < δπ` is the validity gate. **Implementation:** `classifySector` adds `degenerate-principal-sector` verdict (κ=3 path only) and uses `min(δπ, π)` as the κ≥3 boundary — for δ=1 (the common case: `G^{2,1}_{1,3}`, Bessel-K reductions) matches pre-`atip` byte-for-byte; κ=1 path completely unchanged. `MeijerGAsymptoticRefusal.status` union extended; `canUseAsymptotic` adds parallel upstream refusal. New refusal class on the wire (`meijer-g-asymptotic-only/degenerate-principal-sector`); replacement golden 17 exercises it at the exact deleted-golden-17 input. **2δ stored as integer** (avoids half-integer δ representation for κ=2/4/6 with odd p+q). 5 new tests (3 classifier-direct + 2 layer-level) + 1 mutation-prove pin behaviour and the κ=1 admission invariant. ADR-0039 §D6 amendment 1 documents the κ=1 carve-out math story (the bead description had elided the inner-pFq convergence story); §D7 updated. Decision narrowing confirmed via single `AskUserQuestion` after empirical failure signal; no subagents — math reference was already in working set. `bun test packages/meijer-core/ tools/meijer-g*/`: 242 pass / 1 skip / 0 fail. | 2026-05-16 | scientist-workbench-atip (closes) |
| 125 | [ulze: the egf Stokes-band retraction (and atip δ=0 surprise)](125-ulze-stokes-retraction.md) — Close `ulze`: validate the `egf` v0.1 Stokes multipliers at moderate `\|z\|`. Validation found the entire connection-formula assembly is wrong: for v0.1-scope shapes (κ=1, δ≥1 and κ=3, δ≥1), the workbench's `H^{m,n}_{p,q}(z)` (right-closing Slater residue computed by `assembleAlgebraic`) **is G(z) directly** on the principal Riemann sheet — no E contribution needed. The DLMF 16.11.7-9 connection formulas refer to *DLMF's* `H_{q,q}` (formal asymptotic for `{}_qF_q`), NOT the workbench's H. Applying their Stokes multipliers to workbench-H adds a fictitious E contribution. At `\|z\|=10000` (egf shipped goldens) `\|E\| ~ 10⁻⁴³⁵` → invisible bug; at `\|z\|=10` `\|E\| ~ 10⁻⁴` → 2-6 dps disagreement vs 30 dps target. **Phase 0 was a 5-minute orchestrator-direct empirical probe** (not a subagent) that landed the diagnosis: `H_workbench(z) = G(z)` to 30+ dps at multiple test points spanning shape, |z|, and arg z. Phase 1 Opus math-research extended verification across 5 shape families and produced a per-(κ, n, p, q, δ) decision table. Phase 2 Opus coder applied a ~2000 LOC deletion: `exponential.ts` (572 LOC), `stokes.ts` (428 LOC), their tests (792 LOC), the Stokes-band assembly block in `meijergAsymptotic` (~150 LOC), `stokes-band-refused` refusal class, and the `classifyAroundLine` band-cap/sub-precision machinery — all gone. Replaced with one-line `if (verdict.kind === "stokes") return assembleAlgebraic(params, z, z, …, "braaksma-stokes", "stokes")`. Wire labels `method`/`sector` preserved as bookkeeping but the numerical formula is identical to the principal path. **Surprise finding: golden 17 G^{1,1}_{1,3}(δ=0) is silently broken** (kernel +0.0044 vs truth −0.5549, wrong by ~125× AND wrong sign) because `classifySector` uses `κπ/2` as the principal-sector boundary but the correct algebraic envelope is `δπ` (Paris-Kaminski §2.2.2). δ=0 shapes need the full Braaksma E-dominant path — separate bead `atip` (P1 bug). Golden 17 deleted pending `atip`. Verification: math-research test point (`G^{1,1}_{1,1}`, z=10·e^{i(π/2+1)}) matches mpmath to **33-34 dps post-fix** (pre-fix was 5-6 dps). ADR-0039 amended (Amendment 1: 43i regime; Amendment 2: ulze §D3 retraction; §D6: δπ envelope deferred to atip; §D7: post-amendment summary; original §D3 bullets struck-through verbatim for diff visibility). Follow-up bead `ybrw` notes updated (no longer the egf-band-unblocker; reconsider scope). `fc83` (κ=2) flagged for possible scope collapse if H_workbench=G holds for κ=2 (probe pending). `bun run check:quick`: 2032 pass / 4 fail (all 4 pre-existing timeouts: 2 quadrature, 2 contour). | 2026-05-16 | scientist-workbench-ulze (closes); -atip (filed, P1 bug); -ybrw (notes updated) |
| 124 | [Stokes connection coefficients: 43i + egf via ADR-0039](124-stokes-connection-coefficients.md) — Close `43i` (Fox-H truncation fix for `n < p`) and `egf` (Stokes-line connection coefficients via `E_{p,q}` and the multiplier table). **End-to-end orchestrated multi-subagent session**: ten subagent invocations (3 Sonnet audits → 1 Opus deep-research producing `docs/refs/dlmf-16-11.md` + the multiplier-table spec → ADR-0039 drafted by coordinator → 1 Opus coder for 43i → 1 Opus coder for egf Part 1 modules → 1 Opus coder for egf Part 2 wiring → 1 Opus math-research diagnosis of a one-line bug → 1 Sonnet fix + stress tests), single coordinator, serial only. **ADR-0039** settles scope split (43i is purely algebraic truncation; egf is exponential + multipliers; κ=2 deferred to `fc83`), wire-schema extensions (additive `method="braaksma-stokes"`, `sector="stokes"`, two new refusal classes), determinism contract (BigFloat `carg` replaces `Math.atan2`; Berry smoothing via Option C sharp-switch — bigErfc-based smooth version is `ybrw`). **`43i` implementation**: `findOptimalTruncation` split into `findTruncationScan` (Olver §3.7, `n=p`) and `findTruncationParisKaminski` (analytical `N* = ⌊\|κ z^{1/κ}\|⌋`, `n<p AND κ≥1` only — implementation narrowed from ADR's literal "n<p OR κ≤1" after anchor 5 regression analysis; ADR amended). 7 triple-witness goldens (mpmath@35dps + wolframscript@30dps). **`egf` implementation**: two new pure-math modules (`exponential.ts` 572 LOC, `stokes.ts` 428 LOC) + connection-formula wiring; 50 new unit tests on Part 1 modules alone; κ-aware `classifySector` with `SectorVerdict`. **One-line bug caught by triple-witness**: Part 2 subagent shipped κ=1 H-rotation `zArgForH = cneg(z)` per DLMF 16.11.7's `H(z·e^{∓πi})`, producing wrong-by-50-orders-of-magnitude output at golden 19. Math-research subagent localised: DLMF's `H_{q,q}` is the asymptotic for `{}_qF_q`; workbench's `assembleAlgebraic` is the right-closing Slater residue for *G itself* (ref doc §2.2), which equals G(z) on the principal Riemann sheet — no rotation needed. Proof: `H_workbench(−z) = e^{i·2π/3} · G(z)` matched the kernel's bug bit-for-bit. Fix: delete the rotation. Post-fix goldens 19, 20 match wolframscript to 17 dps (capped by 200-term asymptotic limit, not fix correctness). **Multiplier-table residual concern** filed as `ulze`: `±i` constants in `stokes.ts` are unvalidated end-to-end at moderate `\|z\|` because Option-C band guard refuses before reaching the connection formula; needs `ybrw` (bigErfc, narrower band) or diagnostic override to validate. **Pre-existing contour-test timeout** filed as `4kfz`: verified standalone-passing (48.74s) but times out at 60s under bun-test parallel workload; not caused by this session. `bun run check:quick` 2085 pass / 1 fail (the 4kfz timeout). | 2026-05-16 | scientist-workbench-{43i,egf} (closes); -{fc83,ulze,ybrw,uaxz,4kfz} (filed as follow-ups) |
| 123 | [`NTTContext`: instance-scoped NTT plan caches (`nip`)](123-ntt-context.md) — Close `nip`: small-sharp TS-expert refactor. `packages/mod-core/src/ntt.ts` carried three module-level `Map<number, …>` plan caches (twiddles, Bluestein plans, pow2-`n⁻¹`) — unbounded under fuzz, process-global (would collide on `n`-only keys if the modulus generalised), and hidden-timing-coupling (`ntt(x, opts)` timings depended on cache prehistory). **New `NTTContext` class** owns the three Maps as private fields with `clear()` / `size()` API; **`defaultNTTContext()`** is a lazy process-singleton preserving the historical behaviour. Top-level `ntt(x, opts, ctx?)` adds an optional third parameter routing to the default when omitted — backwards-compat preserved, every existing call site untouched. The four helpers (`powerOfTwoTwiddles`/`bluesteinPlan`/`pow2InvNMont`/`nttPow2InPlace`) thread `ctx` through; module-level Map constants removed. **Shape admits future generalisation** to other NTT-friendly primes (readonly `modulus`/`primitiveRoot` already on the class) without breaking any v0.1 caller. Tests: 4 new properties — two contexts have independent caches, `clear()` empties without changing output, `ntt(x,opts)` ≡ `ntt(x,opts,defaultNTTContext())`, singleton identity. Friction-free shard (smallest of session); the only micro-decision was exposing `size()` publicly (yes for testability + fuzz-loop bookkeeping). 29 mod-core tests (was 25; +4); 36/36 ntt goldens byte-identical; `bun run check` 97/0/0. | 2026-05-15 | scientist-workbench-nip (closes) |
| 122 | [`tools/sturm-trace`: TS source → IR Value (`q0b`)](122-sturm-trace-tool.md) — Close `q0b`: the TS-native frontend tool. Take a TypeScript source string that builds a channel via `@workbench/sturm`'s `trace(...)` DSL (ADR-0009), run it in a sandboxed Bun subprocess (ADR-0001 `spawnBun`), emit the canonical IR Value. **Architecture: outer `tool.ts` + inner `runner.ts`.** Outer writes user source to a workspace-resident scratch file (so Bun module resolution finds `@workbench/sturm` by walking up), spawns the runner twice for the default determinism check, bit-compares canonical bytes. Inner dynamic-imports the source, calls the entry, asserts `Channel` return, emits canonical bytes to stdout or typed JSON error to stderr + exit 1. **Four refusal classes** (each `tagged "sturm-trace/<class>"`): `parse-error`, `invalid-when-body` (ADR-0038 — the source-surface fifth layer of the coherent-control closure, complementing the four IR-side layers in `r40`), `non-pure-trace`, `non-channel-return`. **Typed `InvalidWhenBodyError`** in `@workbench/sturm`'s runtime replaces the free-text Error so the runner's catch path uses `instanceof` not regex; source-primitive→IR-op-head mapping (`qbool→prepare`, `qreg→prepare`, `observe→observe`, `ptrace→discard`) lives in the runner because that boundary *is* the tracer's responsibility. **13 goldens** (8 happy: empty/prepare-observe/Bell/GHZ-3/parametrised-ry/symbolic-piOver/m.if-cases/named-entry; 5 refusal: each class + a wrong-entry sub-case). **`--test` hook** pins the round-trip property (acceptance criterion 4): Bell→{00,11}×0.5, GHZ-3→{000,111}×0.5, single-prepare→{0}×1.0 — all within 1e-9. **`bennett-missing` deferred**: `@bennett/core` doesn't exist; user's `oracle` import lands as `parse-error` today. Two key snags: (1) `F.bool` doesn't accept `default` — silently false, caught only via stderr-debug when determinism check never fired on Math.random; lesson: verify *both* subprocesses ran before theorising about caching. Fixed via negative-framed `--skip-determinism`. (2) mkdtemp's random suffix appears in Bun's TS-compile error messages, breaking golden byte-stability — runner now scrubs both the file path and containing dir to stable sentinels before emitting. `non-pure-trace` envelope deliberately carries no diverging-bytes payload (would itself be nondeterministic). **`traceSource` helper factored out** of `def.fn` so `test` calls it with raw strings — eliminates `as never` casts (worklog 118/120 / `0y27`/`qiv8` pattern continued). Full `bun run check` 97/0/0; `wb.sturmTrace(...)` available on the typed barrel. | 2026-05-15 | scientist-workbench-q0b (closes); -can unblocked |
| 121 | [Coherent-control restrictions: ADR-0038 closes r40](121-coherent-control-restrictions-adr.md) — Close `r40`: the tracer-side prerequisite for `q0b` (`tools/sturm-trace`). Decide what happens when a user writes `when(q) { observe(r) }` in TS source, where the IR has no `controls` field on `observe`. **ADR-0038** is the substantive deliverable — it (1) settles the forbidden-op list as **complement-of-{ry, rz}** within the closed seven-head vocabulary (a one-line tracer predicate, not a five-line allow-list), explicitly forbidding `prepare` (debatable but narrowing is forward-compatible) and `oracle` (separable subcircuit needs `when` lifted into its source, beyond tracer scope), (2) names the refusal envelope `tagged "sturm-trace/invalid-when-body"` with payload `record{op_head, control_wires, source_location}` per ADR-0003 boundary-failure shape, (3) **unifies the four-layer IR-level enforcement** that was already in place but never written up — schema closure, builder API, decoder arity, wellformed cases-arm recursion. **Honest finding: no new enforcement code was needed.** A first-pass attempt to add a "non-unitary op has no controls field" check is *vacuous* — the typed `Op` discriminated union has no such field on non-rotation variants; TS won't let you write the violation. The real work was unification + documentation + closing test coverage gaps. Lockstep doc-comments in `nodes.ts` / `schema.ts` / `wellformed.ts` cite ADR-0038; new "Coherent control" README section with the four-row enforcement table. **Test coverage closes two gaps**: (a) 5 new `schema.test.ts` rejections (one per non-rotation head with smuggled `controls` arg — Layer 1 regression guard), (b) `wellformed.test.ts` gains `observe` inside cases arm (rejection logic existed at line 152-154 but had *no* covering test, a real gap) + `falseArm`-position symmetry test (lest a future refactor drop the second-arm traversal). 74 sturm-ir tests (was 67; +7); `bun run check` 95/0 green. Bead `q0b` updated with the decision in notes — next session opening it finds the spec ready to implement against. | 2026-05-15 | scientist-workbench-r40 (closes); -q0b (updated with decision, unblocked) |
| 120 | [`@workbench/json-bridge`: matrix/vector ergonomic helpers (`qiv8`)](120-json-bridge-matrix-vector-helpers.md) — Close `qiv8`: ship `matrixToValue` / `vectorToValue` / `valueToMatrix` / `valueToVector` from `@workbench/json-bridge`. Numerical-tier typed-barrel call sites were re-deriving the same 6-line filter/narrow/`as never` chain to lift `number[][]` to canonical `list<list<float64>>` and back. The forward direction returns the *narrow* `ListValueOf<ListValueOf<Float64Value>>` (not widened to `Value`) so it composes with typed-barrel slots cast-free — paired with `0y27` (`FlagsArgOf` lift), this delivers a fully cast-free numerical-tier call site. The reverse direction takes loose `Value` and refuses with `JsonBridgeError` (`$[i]` / `$[i][j]` path-pointed) on shape mismatch — proving the shape is *exactly* the boilerplate the helper exists to delete. Rectangularity enforced both directions; NaN / ±∞ / subnormals round-trip bit-exact. **Naming normalisation:** the bead proposed `vectorFromValue` which would have mixed "X-to-Y" with "Y-from-X" — TS-expert-want is parallel `valueTo*` in both reverse directions, decided in the design pass. 49 json-bridge tests (was 32; +17 across round-trip, refusal, and *type-level* narrow-return blocks). Dogfood site rewritten in `scripts/demo-scope.ts:339-358` — three lines shorter, no `as never`, output identical. Friction: `list<E>` from `@workbench/protocol` would sometimes widen `E` to `Value` at the return site; fixed by typing the object literal explicitly, with a `ReturnType<…> extends ListValueOf<Float64Value>` static assertion that goes RED on regression. | 2026-05-15 | scientist-workbench-qiv8 (closes) |
| 119 | [`lgamma` / `gamma` real-argument near-pole reflection: zhrm's pair with oj5j](119-lgamma-real-near-pole-fix.md) — Close `zhrm`: the real-argument sibling of `oj5j` (worklog 117). `lgammaRealAbs` carried the byte-identical near-pole cancellation that `clgammaReflect` did; ported the same reformulation (reduce `z → ζ = z − m` before multiplying by π, bump `work` by measured `lossBits`, `m = 0` byte-identical region). **Surprise finding: `gamma`'s real-argument sign-detection had a *different* failure mode of the same bug.** It computed `sgn(sin(π z, work))` purely to read the sign — and near a pole the magnitude got annihilated to zero, `sgn` returned 0, and `gamma` threw a spurious `RangeError: pole at z = ...` for inputs only *close to* a pole, not at one. The right fix here isn't the oj5j reformulation — it's the algebraic identity `sgn(Γ(z)) = (−1)ᵐ · sgn(ζ)` (`z = m + ζ`, `m = round(z)`): exact, sin-free, no near-pole sgn collapse, fewer lines than the pre-fix code. A TS-expert-want answer: the sign is a structural fact about which integer interval `z` lies in, not something we need a numerical computation to discover. **`bigfloat` 267 tests** (was 257; +10 new in `gamma / lgammaRealAbs — near-pole reflection precision (bead zhrm)`). Mutation-proven: reverting `lgammaRealAbs` to `πz`-first → 6/10 RED; reverting `gamma`'s sign to `sgn(sin)` → spurious-throw assertion RED. **Honest scope:** the real `digamma` reflection stub (lines 313-340) carries identical latent code but is gated behind an unconditional `digamma: negative argument support deferred to v0.2` throw — out of scope; whoever fills that stub inherits the oj5j/zhrm template. Friction: bead description named only `lgammaRealAbs`; opening the whole reflection cluster top-to-bottom (Law 1, ground truth before code) surfaced the `gamma`-sign sibling — a subagent given the bead description alone would have missed it. | 2026-05-15 | scientist-workbench-zhrm (closes) |
| 118 | [`FlagsArgOf<D>`: typed-barrel `precision?: bigint` lift for arbprec tools (`0y27`)](118-flagsargof-arbprec-lift.md) — Close `0y27`: worklog 083 (`lc1`/`rn2`) had landed the *runtime* `--precision` contract for `arbprec: true` tools, but the *type level* lagged — `wb.hypergeometricPfq(input, { precision: 50n })` required `{ precision: 50n } as never` because `defineTool` didn't preserve the `arbprec: true` literal through `ToolDefinition`'s static type. **Fix, one file (`packages/contract/src/runner.ts`):** added an `Ar extends boolean = boolean` generic to `ToolDefinition` with the field re-typed `arbprec?: Ar`; mirrored on `defineTool`; rewrote `FlagsArgOf<D>` as a two-tier nested conditional (no intersection) that lifts `precision?: bigint` for arbprec tools and keeps a `Record<string, never>` strict-empty fallback so non-arbprec tools *reject* `{ precision: ... }` at compile time. Two `as never` casts deleted from `compose.test.ts` — the bead's acceptance witness. New `typed barrel — arbprec lift (bead 0y27)` test block: positive `Required<FlagsArgOf<…>> extends { precision: bigint }` (the `Required<>` is load-bearing — without it the assertion is too loose, `{}` trivially extends `{ precision?: bigint }`); negative `@ts-expect-error` pinning that `modPow` *rejects* `{ precision: 50n }`. **Mutation-proven** — dropping the `{ precision?: bigint }` lift turns the positive assertion RED; re-widening the slot to `arbprec?: boolean` turns it RED *and* every typed-barrel arbprec call site RED. **Honest finding:** the `const` on `defineTool`'s `Ar` generic turned out to be *redundant* — regular TS inference already narrows `arbprec: true` to literal `true` whenever the type parameter is constrained `Ar extends boolean`. Kept for defensive consistency with the existing `const Fl`; the real load-bearing piece was always the slot's `arbprec?: Ar` typing, not the `const`. Frictions: positive assertion required `Required<>` to bite (first version passed even when the lift was *gone* — `{}` extends `{ precision?: bigint }`); two iterations on the intersection-vs-conditional structure before the strict-empty negative test passed. 31 compose tests (was 28), 57 contract, 257 bigfloat — no fallout from the `EmptyFlags = Record<never, never>` widening. | 2026-05-15 | scientist-workbench-0y27 (closes) |
| 117 | [clgamma / cdigamma near-pole reflection: catastrophic-cancellation fix (`oj5j`)](117-cgamma-near-pole-reflection-fix.md) — Close `oj5j`: `clgamma`/`cdigamma` lost `≈ (−log₁₀\|ζ\| − 9)` digits of *requested* precision when evaluated near a Γ/ψ pole — `meijer-core` had only been able to *honestly report* the loss (the `7usr` empirical estimator, ~14 dps), not remove it. **Root cause: two compounding cancellations.** The reflection branch formed `π·z` at a fixed `work = prec + 32` *before* `sin`'s argument reduction, so the `π·ζ` information (living `≈ −log₂\|ζ\|` bits below `π·m`) was truncated away — then `reduceModPiOver2` re-did the same large subtraction. **Fix: reduce `z → ζ = z − m` *before* multiplying by π** (`sin(π z) = (−1)ᵐ sin(π ζ)`, `cot(π z) = cot(π ζ)`), localising the one unavoidable cancellation to the single subtraction `ζ = z − m`, and bump the working precision by its *measured* depth `lossBits = max(0, magBits(z) − magBits(ζ))`. `m = 0` (the `Re(z) ∈ (−½,½)` region) is byte-identical to the pre-fix code by construction — `ζ = z`, `lossBits = 0` — so churn is confined to `Re(z) ≤ −½` where the genuine cancellation lives. `cdigammaReflect` fixed in the same edit (identical root cause, same file — Rule 2). Kept the `cexp(±iπζ)` structure rather than a first-principles `csin` *specifically* to keep `m = 0` bit-identical; the residual `exp(−x)−exp(x)` cancellation is bounded by `lossBits`, so the `work` bump already pays for it. `bigfloat` 257 tests (was 245; 12-test near-pole block, mutation-proven — the `πz`-first form collapses the `ε = 1e-69` cases to ~21/~41 digits at 30/50 dps). Real-argument sibling (`lgammaRealAbs`/`special.ts`) carries the identical latent bug — filed as `zhrm`, honest decomposition not scope creep. Friction: `CLAUDE.md`'s "`bun run check` ~25s" is badly stale — `contour.test.ts` alone is 4m26s. | 2026-05-14 | scientist-workbench-oj5j (closes); -zhrm (files) |
| 116 | [cone-core convergenceTest hook: termination denominated in the consumer's residual (`oxuk`)](116-cone-solve-convergence-test-hook.md) — Close `oxuk`: `cone-core`'s `scsSolve` decided `optimal` from O'Donoghue's §3.5 test (embedded translated problem, 2-norm, gap term), but `tools/cone-solve`'s `precision` contract is the §C-wire-form KKT residual of the *recovered* point (∞-norm, `xᵀs` term) — they differ by a per-problem 0.59–3.08× factor, so the embedded test fired while the §C residual still exceeded the request, and `rgl8`'s stopgap pessimistically re-labelled `optimal → iter-cap`. **Fix: an optional `convergenceTest?: (c: Candidate) => boolean` on `recoverPrimalDual` + `SCSOpts`.** Absent (the default) = paper-faithful §3.5, substrate semantics fixed. Supplied = sole arbiter of `optimal` (certificate branches untouched). `cone-solve` hands `scsSolve` a closure that recovers the §C point and returns `kktResidualC ≤ precision`, so the iteration is *driven to the wire criterion directly* — `rgl8`'s coherence-guard re-label retired; `optimal` means `optimal` with no asterisk. The two rejected options are in the ADR-0030 addendum: cone-core adopting the ∞-norm/`xᵀs` form is **structurally impossible** (it never holds the original §C `(A,b,c)`, only the translated embedding); a per-problem amplification estimate is a Rule-2 bandaid (the ratio is the unpredictable thing). The `Array.prototype.sort(comparator)` move — substrate owns the machinery, caller owns the criterion. `cone-core` 99 tests (was 90; new hook blocks in `hsde`/`scs` tests), mutation-proven (hook ignored → 4 fail, hook inverted → 5 fail). cone-solve `--test` gains an `optimal-precision-coherence` assertion; 14 goldens unchanged. **Bead acceptance — profiler optimal-rate climbs: 3/5 → 5/5** (`blend` driven ~800 extra iters to `optimal` at 9.72e-7, `scsd1` 12 more iters at 8.00e-7; over-claimed-precision 0). ADR-0030 addendum; `bun run check` green. | 2026-05-14 | scientist-workbench-oxuk (closes) |
| 115 | [cone-core: SOC + PSD cone projections (`0wc7`)](115-cone-soc-psd-projections.md) — Land the second-order and positive-semidefinite cone projections in `packages/cone-core` — five of seven cone families now live, the keystone unblock for the SDP side of the cone tier. **Law 1 first:** staged `docs/refs/parikh-boyd-2014-proximal-algorithms.pdf` (the 2016 SCS paper defers projection *formulas* to Parikh-Boyd §6.3) and transcribed §6.3 to a new ground-truth file `docs/ground-truth/convex/cone-projections.md` — the Moreau decomposition, the SOC three-case closed form (§6.3.2), the PSD eigenvalue clamp (§6.3.3 eq 6.6), and the derivation of *why the √2 svec scaling is load-bearing*. **SOC** (`projectCone`/`inCone` `case "soc"`): Parikh-Boyd's vector-first formula re-derived in cone-core's scalar-first `(t, x)` ordering; three branches (already-in / polar-to-apex / boundary), branch order load-bearing for the `ρ=0` corner. **PSD** (`case "psd"`): the block is the upper-triangular √2-scaled `svec`; projection is `smat → eigh → clamp the negative spectrum → svec`, reusing `linalg-core`'s real-symmetric `eigh`. The √2 is *the correctness argument* — it makes `svec` a Frobenius isometry, which is what licenses coordinate-wise Euclidean projection = matrix projection (ADR-0030 OQ4, now resolved; the classic amateur-SDP bug). New `soc`/`psd` smart constructors (reject dim 0 — a SOC needs its scalar apex). The four `0wc7` `ConeError` stubs gone; `ExpCone`/`PowCone` stubs remain (re-pointed at `j282`). 45 cone tests (was ~30) — idempotence, range, Moreau `z = Π_K(z) − Π_K(−z)` with orthogonal summands, non-expansiveness, exact-value goldens incl. the off-diagonal PSD case that distinguishes correct √2-svec from plain stacking. **Mutation-proven** — four perturbations each confirmed RED (`SQRT2→1`, SOC scale dropped, SOC scalar sign-flip, PSD clamp `λ>0→λ≠0`). `scsSolve` is cone-agnostic by construction, so it now solves soc/psd problems with only a setup-gate re-scope (`assertProjectable` gates exp/pow alone); two end-to-end SOC + PSD solves with hand-derived optima confirm the integration. `cone-core` 90 tests (was 72), `bun run check` green. Tool-layer PSD *wire form* (`tools/cone-solve`) is its own bead. | 2026-05-14 | scientist-workbench-0wc7 (closes); -j282 remains |
| 113 | [tools/cone-solve, §5 scaling, and Anderson acceleration (`2ivi`, `k9mm`)](113-cone-solve-and-acceleration.md) — Build `tools/cone-solve` (the universal-primary cone tool, ADR-0030 §C/§D wire over the `cone-core` substrate: §C→O'Donoghue translation, §C primal-dual recovery, the discriminated `SCSResult`→§D record, five-class status taxonomy, refusal envelopes, 14 goldens, `--test`). Grading it against `lp-netlib` surfaced a **structural finding**: plain SCS does not converge on NETLIB in tractable time (`afiro` ~3k iters to 1e-8, `adlittle` ~117k, bigger problems never) — exactly the slow-tail convergence O'Donoghue 2016 §1 describes. Added both literature levers to `cone-core`: `scaling.ts` (Ruiz data equilibration, O'Donoghue §5 — iterate on the rescaled `Â=DAE`, judge termination on the original residuals) and `anderson.ts` + ADR-0036 (Type-II Anderson acceleration, Zhang-O'Donoghue-Boyd 2018 / Walker-Ni 2011 — a generic windowed-least-squares extrapolator, on by default). The accelerator is **proven correct in isolation** (2292→4 and 23015→8 iteration collapses on known contractions; mutation-proven) — and a ridge bug (`max(1,…)` floor swamping the signal in SCS's slow tail) was caught *because* it's testable standalone, then fixed. **The load-bearing lesson:** even correctly accelerated, AA-II cannot make a pure-SCS universal solver bench-competitive on the small/medium NETLIB tier — not at 1e-8, not even at the 1e-6 ADR-0030 ceiling — because the SCS map is *nonsmooth* (the reason ZOB's actual contribution is Type-I + Powell globalisation). That is structural and is what ADR-0030 §B's specialist tier (`lp-solve`, which already passes lp-netlib at 1e-8) exists for. `cone-core` 72 tests pass; `cone-solve` correct end-to-end (`afiro` → optimal, consensus objective); `bun run check` green. The bench reconciliation (re-profile the verifier to 1e-6 / accept a partial gate / invest in Type-I) is an open project-direction question surfaced for the user. `k9mm` (AA implementation) complete; `2ivi` stays in progress pending that call. | 2026-05-14 | scientist-workbench-k9mm (closes); -2ivi (in progress); ADR-0036 |
| 112 | [packages/cone-core: the LP-complete slice of the SCS substrate (`cp9k`)](112-cone-core-lp-slice.md) — Ship `packages/cone-core`, the Phase-0 substrate of the convex-cone solver epic (`eg9j`, ADR-0030 §H) — the SCS / first-order lineage, distinct from `solver-ipm`'s interior-point HSDE. Three modules ported from O'Donoghue-Chu-Parikh-Boyd 2016 (`docs/refs/odonoghue-2016-scs.pdf`, transcribed to `docs/ground-truth/convex/scs-algorithm.md` — Law 1): `cones.ts` (the `Cone` union + `projectCone`/`dualCone`/`inCone`), `hsde.ts` (`buildHSDE` + the dense skew-symmetric `assembleQ` + `recoverPrimalDual`, the §3.5 termination evaluator factored out as a pure tested kernel), `scs.ts` (`scsSolve` — the §3.2.3 three-step iteration with §3.3 over-relaxation, §3.4 zero-avoiding init, §4.1 Sherman-Morrison-Woodbury factorisation-caching subspace solve). **Reading all 27 paper pages first revealed the scope boundary**: the paper gives the complete algorithm but defers cone-*projection formulas* to Parikh-Boyd §6.3 (not staged) — which lines up exactly with the LP-only v0.1 bench gate. So v0.1 ships the LP-complete cone subset (nonneg/zero/free — definitional projections), with SOC/PSD/Exp/Pow in the `Cone` union but throwing a loud `ConeError` naming real sub-beads `0wc7`/`j282` (Rule 8 honest scope). `SCSResult` is a discriminated union keyed on `status` (the type forbids reading `objective` off an `infeasible` result); depends on `linalg-core` only, not protocol+contract (a deviation from ADR-0030 §H's stale line — a numerics substrate speaks `Float64Array`, the wire encoding is a tool-layer concern, exactly as linalg-core does). 62 tests, mutation-proven with four sign-flips (Moreau/skew-symmetry/SMW/KKT each confirmed RED); hand-verified LP optima cross-checked by independent KKT re-derivation; determinism is bit-identical. Unblocks `2ivi` (`tools/cone-solve`). | 2026-05-14 | scientist-workbench-cp9k (closes); -0wc7 -j282 (sub-beads filed); unblocks -2ivi |
| 111 | [`tagged` / `taggedSchema` preserve their payload type (`x0lc`)](111-protocol-tagged-narrowing.md) — Close `x0lc`: make the `tagged` value constructor and the `taggedSchema` schema constructor generic in their payload, mirroring the already-generic `list` / `record` / `listSchema` / `recordSchema`. A helper that builds tagged values now keeps the narrow payload type (`tagged("t", int(0n))` is `{kind:"tagged"; tag:string; payload: IntegerValue}`) instead of widening to `TaggedValue` and forcing an `as never` cast at the call site — the "extract a helper" reflex stops being punished, per the two principles. **Law-1 ground-truth check caught the bead two-thirds stale**: it claims `list`/`record` widen, but they were already fixed to be generic; the real remaining gap was `tagged` alone. New `TaggedValueOf<P>` interface beside `ListValueOf<E>`; `taggedSchema` returns `Schema<TaggedValueOf<P>>` so `ValueOf<…>` threads the payload type through tool I/O inference. Single generic signature, no fallback-overload pair — the narrowed return is a covariant subtype of the wide type, so it subsumes every `Value`-typed call site (typecheck clean across the whole workspace, **zero call-site updates needed**). New compile-time canary in `schema.test.ts` (2 tests, 98→100 protocol pass): typed-assignment + member-access that fails `tsc` if either constructor widens. Mutation-proven — reverting `tagged` to the wide signature turns the canary RED *and* breaks `demo-scope.ts` + `choi-iso/tool.ts`, confirming the narrowing is already load-bearing. Unblocks `qiv8` (json-bridge matrix helpers can now return narrow types). README "Constructing values" paragraph added; consistency refinement within ADR-0004, no new ADR. | 2026-05-14 | scientist-workbench-x0lc (closes); unblocks -qiv8 |
| 110 | [HSDE Phase 5 Tier 1: iterative refinement on the Schur back-sub](110-solver-ipm-hsde-ir.md) — Close `vajd`: new `solveWithIR` (ECOS/Higham/SDPT3-pattern iterative refinement) wired into all six Cholesky back-substitution sites of `HsdeLpSolver` and `HsdeNtSdpSolver`, sharpening the regularised factor back toward the *unperturbed* Schur system. Two-workspace dance fixes the deliberately-buggy undo path in the HANDOFF §4.2 sketch — `workE` carries the residual, `workCorr` retains the correction so the in-place rollback is exact. Tier-0's `nitref1/2/3` trace slots stop being stubs. New `hsde-precision.test.ts` (8 probes) targets `solveWithIR` directly on hand-built systems with a known true solution: ill-conditioned → IR fires & strictly improves the residual; exact factor → `nitref=0`, bit-identical to plain back-sub; poor preconditioner → overshoot rolled back, no harm. Mutation-proven (residual sign-flip + defeated rollback both confirmed RED). 113 existing tests pass — zero regression = "same wire output as Tier 0". `hinf2` diagnostic: IR **unlocks the precision floor** — Tier-0's trajectory was locked at `pInf≈1.26e-7`, with IR it moves freely through `1e-8`–`1e-10` and touches `~4e-10`; returned `pInf` improves ~14× (`8.1e-7`→`5.6e-8`). Clean-`optimal` termination on `hinf2` is honestly deferred to Tier 2/3 (the good iterates aren't yet recognised as terminal — best-iterate/termination work). Unblocks `fsr7` (Tier 2), `lniy` (Tier 3). | 2026-05-14 | scientist-workbench-vajd (closes) |

## How to add a new shard

1. Pick the next number (`00N-<short-slug>.md`).
2. Use the structure: **Context → What changed → Why these choices →
   Frictions surfaced → Acceptance → Pointers**.
3. Aim for ~200 lines. Prose-dominant, code blocks for diff highlights only.
4. Add a row to the table above.
5. If the shard introduces an architectural decision, file a paired ADR
   under `docs/adr/` and reference it.

## Cross-references

- ADRs: `docs/adr/`
- Issue tracker: `bd list --status open` (beads, stealth-installed)
- Memory (cross-session): `~/.claude/projects/.../memory/`
- Agent guidance: `CLAUDE.md` at repo root
